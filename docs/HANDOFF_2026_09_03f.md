# Session Handoff — 2026-09-03f

## What happened

Continuation of 2026-09-03e. Git at 7210e01 (pushed) on entry. EMI results
already complete and committed.

### Root cause identification: Mamba state continuation gap

Traced the EMI divergence to a HuggingFace implementation gap in Falcon-H1's
`torch_forward` method:

1. **Line 819** of `modeling_falcon_h1.py`: `previous_states = torch.zeros_like(states[:, :1])`
   — the inter-chunk SSM initial state is hardcoded to zeros for multi-token
   continuation (`seq_len > 1`).

2. **Line 705**: Convolution applied via `self.conv1d` without incorporating
   the cached conv state (uses nn.Conv1d zero-padding instead).

3. Both `mamba_chunk_scan_combined` and `causal_conv1d_fn` accept `initial_states`
   parameters — the Falcon-H1 model code never passes them for `seq_len > 1`.

4. The CUDA path (`cuda_kernels_forward`) has the same gap.

5. Single-token decode (`seq_len == 1`) correctly uses cached states (lines 686-762).

### Minimal verification

Wrote and ran `scratchpad/verify_mamba_cache_bug.py`:
- One-shot vs two-shot (multi-token): **TV = 0.118**, max logit diff = 6.527
- One-shot vs single-token decode: **TV = 0.000000** (exact match)
- Cross-prefix two-shot convergence: TV = 0.794 (Mamba ignores prefix)

### Scope of impact

- **ALL SVB experiments on Falcon-H1** used the buggy multi-token cached
  continuation pattern (`get_state_after_prefix` → `get_dist_from_state`).
  They measure behavior where Mamba layers don't see the prefix — only
  attention layers carry context.
- **ALL suffix algebra experiments** (legacy and current) used the same pattern.
- **SVB-Qwen3** (pure transformer) is UNAFFECTED — KV cache is exact.
- **SVB competence staircase** used full-text mode — CORRECT.
- **EMI Mode F** (full text) — CORRECT. Suffixes are semantically neutral.
- Cross-architecture comparison (Falcon-H1 vs Qwen3) is invalid.

### Corrected measurement in progress

Modified `run_svb_0.py` to support `use_decode_mode` flag (single-token decode).
Running `verify_svb_decode_mode.py`: compares buggy multi-token, correct
single-token decode, and full-text modes on SVB depth 1-2 with suffix s0/s1.

## Key finding: Qwen3 settling time is REAL

Since Qwen3 is a pure transformer (no Mamba, no bug), its SVB results are
ground truth. The settling time effect is genuine:

| Depth | s0 (σ) | s1 (σ) | Gain |
|-------|--------|--------|------|
| d1    | 0.958  | 0.970  | +1.2% |
| d2    | 0.897  | 0.945  | +4.9% |
| d3    | 0.792  | 0.900  | +10.8% |
| d4    | 0.753  | 0.880  | +12.6% |

Settling time scales with depth — the transformer needs extra processing
steps to resolve deep scope structure. This IS a native latent-space property.

## Current state

- **Git:** main at 2907dbc (pushed). Uncommitted: `run_svb_0.py` (decode mode
  support), `verify_svb_decode_mode.py` (new verification runner).
- **SVB decode verification:** RUNNING (PID from background task bludh5g5b).
- **Codex API:** Down with sustained 404 errors (3+ attempts). No Codex
  review possible this session.
- **Blackboard:** e856-e858 recorded (root cause, verification, reinterpretation).
  Signals s63-s65 addressed, s66-s69 opened.

## Restart procedure

1. Read AGENTS.md, CLAUDE.md, STATE.md, NOTEBOOK.md (top entry: MAMBA STATE BUG).
2. Check if `verify_svb_decode_mode.py` completed; read results from
   `experiments/results/svb_decode_verification.json`.
3. If decode/full-text sigma matches (TV < 0.01): single-token decode is
   validated as correct. Rerun full SVB-0 on Falcon-H1 in decode mode.
4. If decode/full-text sigma differs: investigate further.
5. Try Codex evidence gate (API may have recovered).
6. Strategic direction: settling time on Qwen3 is real and scales with depth.
   This is the most promising lead for native math. Characterize the functional
   form σ(suffix_count, depth) more precisely.

## What's next (priority order)

1. **Read SVB decode verification results** when complete.
2. **Fire Codex evidence gate** when API recovers — audit the Mamba state gap
   finding and the Qwen3 settling time analysis.
3. **If decode mode validates:** Rerun full SVB-0 (all depths, all suffixes) on
   Falcon-H1 in decode mode for comparison with Qwen3.
4. **Settling time characterization:** What is σ(s, d)? Exponential approach to
   limit? How many suffix tokens for full settling? Same curve across models?
5. **Theory:** What does settling time mean architecturally? Why does the
   transformer need extra serial processing steps for deep scope resolution?
