# Latent-Space Reasoning — pause handoff, 2026-08-30

> **PAUSED.** No experiment is authorized merely by reopening this repository.
> Read this document, `STATE.md`, and the adopted audit #39 record before
> proposing work. Any restart begins with the blackboard and a fresh direction
> dialogue.

This is the front door for anyone — human or coding agent — arriving with no
context. It tells you what the program is, exactly where it stopped, what has
already been tried and closed (do not repeat it), what the standing decision
rule is, and the operating rules that bind any restart. Audit-licensed scientific wording is quoted or linked to its source. The arc,
repository map, and operating sections are navigation summaries and must not
be cited as findings. If local `AGENTS.md` is present, it governs process;
`STATE.md` governs scientific claims.

## Mission and guiding question

**Program (opened 2026-08-27): the native mathematics of latent spaces.**
Embedding and latent spaces are not number systems. This program builds their
mathematics from axioms upward — what a latent space is, which relations and
operations are meaningful — and derives definitions and propositions from
there, Euclid-style, rather than porting ℝⁿ mathematics onto embedding vectors.
Classical goals carry over (measure closeness, update on evidence, compose,
infer, prove); the native constructs may look nothing like their classical
counterparts.

> Mathematics was invented by inhabitants of a world to navigate it — counting, measuring, mapping, predicting — and its laws were shaped by what that world made necessary. Invert the dynamic: **take the latent space as the world.** Ask what a denizen of that world would have to invent to find its way — what counts as the same place, what a move is, what effort a move costs, what a map is, what regularities make prediction possible — and let that need decide which primitives and laws we build.

> The mission remains open. No tested real-model construction has established native latent mathematics, a transferable causal state, or a general impossibility result.

## Exact pause point

The last completed measurement is `register_bridge_preflight_v1`, a noncausal
feasibility measurement—not staircase advancement or a causal/code-level
bridge (locked `8beb8e9`; ledger `register_bridge_preflight_v1_lock`,
`register_bridge_preflight_v1_result`, and
`register_bridge_preflight_v1_audit39`).
Its audited wording, verbatim from the adopted audit #39 block in `STATE.md`:

**`register_bridge_preflight_v1` — PREFLIGHT PASS — EXPLICIT-LEGEND STATE LINEARLY DECODABLE (audit #39: UPHOLD; noncausal feasibility result, not a code-level or causal bridge).**

`register_bridge_preflight_v1` — PREFLIGHT PASS: EXPLICIT-LEGEND STATE LINEARLY DECODABLE. In frozen Qwen3-1.7B-Base, cross-fitted rank-≤8 Ridge decoders evaluated on held-out entities × templates × a disjoint balanced permutation bank achieved 0.815 accuracy (entity-bootstrap LB 0.779; folds 0.828/0.852/0.766; minimum state recall 0.615), versus input-embedding 0.125, categorical 0.135, paired reassigned-legend original-state 0.016, and shuffle-null p99 0.204; the intact decoders followed the paired legend’s newly denoted state at 0.852, ruling out fixed tag identity. This is a noncausal explicit-legend lookup signal, not a code-level or causal bridge. The program remains paused; on restart, conduct the required direction dialogue and then one held-out causal residual-to-writer-centroid injection test, with no synthetic staircase advance or further decoder sweep first.

Licensed sentence (audit #39):

> In frozen Qwen3-1.7B-Base revision `ea980cb0a6c2ae4b936e82123acc929f1cec04c1`, a predeclared rank-≤8 cross-fitted linear decoder read the explicitly legend-denoted state from the two-token record-tag residual under held-out entities, two held-out templates, and a disjoint balanced permutation bank at 0.815 accuracy (folds 0.828/0.852/0.766, entity-bootstrap lower bound 0.779, minimum state recall 0.615), versus 0.125 input-embedding, 0.135 categorical, and 0.016 paired reassigned-legend original-state controls and a 0.204 shuffle-null p99; on the paired reassigned legends the unchanged-tag decoder followed the newly denoted state at 0.852, establishing a noncausal, prompt-family-bounded explicit-legend state signal—not a code-level or causal bridge, persistent register, synthetic-consumer capability, or native latent mathematics.

Never say (audit #39):

- “Qwen learned a register.”
- “This establishes a causal bridge.”
- “The Qwen residual already contains the constructed consumer’s code.”
- “The state survived, persisted, or was remembered.”
- “The result establishes an eight-dimensional state subspace.”
- “The decoder reads tag identity.”
- “The destroyed arm contains no state information.”
- “The destroyed context failed.”
- “Template and permutation transfer were independently established.”
- “The result generalizes to arbitrary templates, legends, or permutations.”
- “The shuffle null reran the entire selection pipeline.”
- “The bootstrap includes decoder-training uncertainty.”
- “Every layer contains the signal.”
- “The legend-occurrence reference is a gated control.”
- “The residuals can drive the frozen synthetic consumer.”
- “This demonstrates semantic facts, an autonomous state, or native latent mathematics.”

Continue-or-not / highest-leverage ruling (audit #39):

**Continue conditionally; the predeclared PASS branch should apply.**
The highest-leverage next move is the required 2–3-round direction dialogue followed by one held-out causal bridge test: map Qwen record-span residuals into the successful writer centroids and inject them into the frozen constructed consumer.
The program should not continue through another synthetic rung, decoder characterization round, layer sweep, or prompt repair. The current pause remains appropriate until the dialogue produces a locked causal test.
The program is still tunnel-visioned around an eight-symbol explicit-lookup micro-world. The PASS justifies one causal discriminator; it does not justify extending the ladder indefinitely.

Measurement-to-artifact ratio at the pause (audit #39):

Current runner: 104 nonblank lines. Under the repository’s existing line convention, approximately 34 are generic apparatus and 70 are estimand-bearing measurement logic, about **0.49:1**. That is below the line warning, but this runner builds no causal central artifact; it remains a measurement.
Round accounting:
- Audit #38 baseline: 21 measurement/governance : 7 build.
- Round 28 lock review: 22:7.
- Preflight result: 23:7.
- This audit: **24:7 = 3.43:1**.
The ratio remains above the 2:1 warning and below the 5:1 mandatory halt. The next active round must build the causal bridge discriminator or close the line; another measurement-only round is not justified.

Never cite the raw `run_result.json` status string of any run without the
audit qualification that narrowed it.

## One-screen program arc

| Line | Status | Binding provenance |
|---|---|---|
| Prior perturbation/diffusion program | Closed 2026-08-27; nested-arithmetic claims withdrawn after the benchmark was shown to measure termination under a token cap | `legacy/README.md`; `legacy/docs/CORRECTION_NESTED_ARITHMETIC_2026_08.md` |
| NLM-007 residual-dynamics line | Closed by terminal allocation rule, not by a scientific null | Audit #22; ledger `nlm007_closed_audit22`; `STATE.md` "NLM-007 — CLOSED" |
| Toy operational-quotient program | Ended; no learned artifact passed the complete exact reducer, and Round 37 found no architectural win | Audits #23–#26 plus the Round 37 audit; ledger ids enumerated in `STATE.md` "Closed toy program" |
| Frozen-model coordinate/interchange/state-bus/control-cost line | Stopped as an allocation pivot, not a scientific conclusion | Audits #27–#31; ledger `direction_r10_program_ruling` and the construction rows |
| One-write positive-control staircase | Exact constructions failed or localized instrument, extraction, actuator, or prompt/output-geometry constraints; no general memory/capacity conclusion | Audits #32–#36 |
| Constructed register substrate | Rung 0 is a qualified synthetic oracle-code selector; rung 1 is a qualified answer-supervised eight-symbol writer; neither establishes semantic writing, persistence, geometry, or pretrained-model structure | Audits #37–#38 |
| Real-model bridge preflight | Noncausal feasibility PASS (audit #39 UPHOLD): explicit-legend state linearly decodable at the record span under held-out entities, two held-out templates and a disjoint balanced permutation bank; not a code-level or causal bridge | Ledger `register_bridge_preflight_v1_lock`, `register_bridge_preflight_v1_result`, and `register_bridge_preflight_v1_audit39` |

The durable findings of the frozen-model line, verbatim from
`docs/STRUCTURED_NEGATIVE_2026_08_29.md` ("Findings that survive audit"):

- "Under the current apparatus budget, this sequence of frozen residual-stream constructions is not yielding a native-mathematics artifact, so open-ended layer, task, and decision-rule repair stops here. This is an allocation pivot, not evidence that frozen pretrained residual streams lack usable native structure."

- `state_bus_v1r1` licensed "repeatedly maintained supervised response control with donor-verbalizer-specific lexical/semantic steering," not autonomous persistence, abstraction, general interchangeability, or native latent mathematics.

- `onewrite_recall_v1` closes "the fixed model/layer/source-state/16-dimensional encoder/injector/norm-cap/slot/objective/400-step/prompt construction—not one-write memory, persistence, block-12 capacity, or persistent state in real models generally."

- For the rung-0/site-oracle pair: "At this exact site and prompt, finite strict-choice response is strongly tag- and context-dependent. Removing the learned shared J does not remove the concentration of effects, so J optimization was not its sole source; nevertheless, the concentration remains inseparable from baseline logit gaps, cross-name gradient averaging, local-to-finite extrapolation, the fixed norm, and the first-token greedy readout." `reachability_v1` (audit #36, verbatim): `reachability_v1` is a preregistered measurement with classification `NO SLOT-SPECIFIC GEOMETRY CONCLUSION`: at the fixed block-12 final `Internal record:` slot in frozen Qwen3-1.7B-Base, 42–43 tokenizer-position steps before the queried logits, the mean over 24 training names of the \(0.25\|h\|\)-scaled centered Jacobian of eight tag first-token logits had top-mode energy 0.638 (name-bootstrap 95% CI 0.624–0.652), participation effective rank 2.279 (2.197–2.362), \(\sigma_2/\sigma_1=0.450\), and \(\sigma_3/\sigma_1=0.389\); a read-only audit recomputation found typical per-name geometry of similar width and 0.895 mean-energy coherence, so averaging did not create the result, while the eight preselected instruction-token position controls spanned top energies 0.579–0.731 and effective ranks 1.809–2.664 and all four fixed displayed-tag-order permutations produced broader spectra; at one finite dose the top three shared slot directions had median predicted-versus-realized centered-logit cosines 0.995, 0.944, and 0.995 and substantial secondary-mode responses, but sign-dependent magnitude and decoded-boundary asymmetries remained, so the result licenses a coherent several-mode local response under this exact prompt, vocabulary, site, layer, and dose, not a slot-specific dimension, a hard reachability limit, equivalence to generic positions, an explanation of the earlier two-tag pattern, or evidence about memory capacity.

The strongest unified explanation is correspondingly local and plural. Several frozen-model effects are parsimoniously explained by cue-redundant lexical-semantic response geometry rather than persistent state. Small base models could not support the more demanding rule-application instruments, although direct visible recall was valid. The chosen generic source anchor carried only weak tag information. Finally, the actuator's apparent anisotropy was construction-bound and was not isolated from prompt, label-order, baseline-gap, or output-boundary geometry. These are four constraints, not one universal latent-space hole.

> The sequence did not show that the block-12 slot is too low-dimensional to carry state; it showed that this exact frozen-model interface never converted weak, presentation-bound source and response geometry into balanced, content-specific causal control, even though the slot has a coherent several-mode local effect on the registered output logits.

## Do not repeat

Every exact recipe below is closed to rerun. The rows include construction
failures, a measurement, qualified synthetic calibrations, and a noncausal
preflight; those scientific statuses are not interchangeable. The right-hand column is a pointer to the
binding licensed sentence and never-say list in `STATE.md`; it is not
permission to paraphrase.

| Construction | Closure reason | Licensed/never-say authority |
|---|---|---|
| NLM-007 | Terminal allocation stop; Round 34b inconclusive | `STATE.md` NLM-007 block, audit #22 |
| Operational quotient v1/36b/36c/36d and presentation quotient | Toy program ended; exact certificates diagnostic-only; Round 37 no architectural win | `STATE.md` closed-toy blocks, audits #23–#26 / Round 37 |
| `coordinate_v1` | `UNINTERPRETABLE — INVALID POLARITY BASELINE` | `STATE.md` historical coordinate block |
| `coordinate_v2` | Killed at the predeclared baseline gate | `STATE.md` historical `coordinate_v2` block |
| `coordinate_v3` | Coordinate claim negative; narrow late lexical-control diagnostic only | Audit #27 block |
| `interchange_v1` | Locked raw-zero baseline failed; no swap arm ran | Audit #28 block |
| `interchange_v2` | Fixed single-anchor replacement construction failed | Audit #30 block |
| `state_bus_v1r1` | Fixed-construction fail; repeated supervised response controller, not autonomous state | Audit #29 block |
| `control_cost_v1` | Fixed actuator/solver/readout/budget construction failed; censored costs void broader laws | Audit #31 block |
| `onewrite_state_v1` | Killed pre-lock because the visible-fact instrument failed; no state hypothesis tested | Current statement |
| `onewrite_recall_v1` | Valid instrument, but the registered held-out channel failed | Audit #32 block |
| `onewrite_recall_rung1` | Training-item construction failed; failure initially unlocalized | Audit #33 block |
| `oracle_actuator_rung0` | Exact shared-J oracle actuator failed | Audit #34 block |
| `site_oracle_v1` | Balanced bounded eight-way control failed; exact slot/prompt recipe closed | Audit #35 block |
| `reachability_v1` | Measurement classified `NO SLOT-SPECIFIC GEOMETRY CONCLUSION` | Audit #36 block |
| Constructed register rungs 0–1 (`necessary_register_v1`, `necessary_register_rung1`) | Qualified synthetic calibrations; synthetic staircase stopped after audit #38 | Audits #37–#38 |
| `register_bridge_preflight_v1` | No repair or rerun; use audit #39 | Audit #39 |

> `necessity_navigator_v1` is **deferred, not a result and not automatically next**. Its 2,000-step record is a smoke only. Running it requires a fresh direction dialogue.

## Standing decision after the preflight

Audit #39 supersedes everything in this section; its ruling is copied verbatim
under "Exact pause point" above. The branches pre-declared by audit #38, which
audit #39 either preserves or replaces:

- PASS → direction dialogue, then one held-out causal bridge test mapping real-model residuals to successful constructed-writer centroids and injecting them into the frozen constructed consumer.
- PARTIAL → lexical/context engineering evidence only; no staircase advance.
- FAIL → close the synthetic register route as nonbridging through this tested source-span path.
- Never reopen layer, site, actuator, prompt, or model-size sweeps as repairs.

## Binding operating contract

`AGENTS.md` (the local operating file) is git-ignored and will not be in a
fresh clone. These are its binding rules, reproduced here:

- Blackboard (Iqidis MCP; local `.blackboard/`, absent from a fresh clone): ONE mission board `5df235ea` for direction/theory work and ONE audit board `e5a4e16f` for fresh audits. Every Codex prompt names its board and says `bb_get_state` on it first, add entries with supports/contradicts/supersedes links, `bb_synthesis` on it before concluding — never `bb_create` (that mistake produced 24 one-off boards before 2026-08-30). If the tools are absent, say so and fall back to `STATE.md`/`NOTEBOOK.md`.
- Before any run, state the expectation, what each possible outcome implies, and the simplest confound that could explain every row.
- Before fitting or interpreting a residual-stream transition law, run the identity-plus-shared-displacement null on the same held-out splits, endpoints, support accounting, and clustered gates. If the target is a difference, make Δ = Y − X and its mean displacement the primary decomposition before adding model capacity.
- Hard evidence gates also require: the cheapest direct remedy as an arm before scaling; a fixed-input numerical-noise null for diversity claims; held-out tasks for every headline; model revision and chat-template version pinned in every result; and no external claim until the required controls have actually run.
- Automated latent-scorer values are evolutionary guidance, not quality evidence. Quality claims require manual review of decoded outputs with the reason for manual review stated.

- A scientific run requires a frozen design, config, runner hash, thresholds, statuses, controls, and kill rule — recorded as a `lock` row in `experiments/ledger.jsonl` before launch.
- A locked outcome is audited once, by a fresh, unprimed Codex auditor with no prior context, in both directions (over-claimed wins and over-claimed kills). Its language corrections are adopted verbatim. There are no outcome-dependent rescue or repair runs. A scientifically different successor requires the 2–3-round direction dialogue and a new lock.
- Exact certificates are diagnostics only. Learned continuous-model verdicts require tolerance criteria, effect sizes, and seed spread.
- Every direction requires a lay one-sentence "so what"; the cheap-trick explanation must be run as a control, not imagined.
- The central artifact must be intervention-demonstrated structure in a real model's latent space. Toy worlds are allowed only as a one-round control for a claim about a real model.
- After two consecutive rounds produce only apparatus and negatives, say "this is not working" to the user. Liveness is not progress.

- Use the positive-control staircase: training items/zero delay → short delay → held-out names → unseen wording → long delay, changing one difficulty per audited rung.
- Report apparatus/artifact lines and measurement/build rounds at every heartbeat (the ratio tripwire). Above 2:1, warn; above 5:1, halt and pivot.
- Generation accuracies always include termination. Task-nested samples use paired or clustered inference.
- Audit wording overrides raw runner status. Propagate an adopted downgrade or qualification to `README.md`, `STATE.md`, `NOTEBOOK.md`, `experiments/EXPERIMENTS.md`, and the ledger in the same session.
- One CPU experiment process at a time. No GPU run without Devansh's explicit approval.
- Codex means the real OpenAI Codex CLI, never a simulated review or a sub-agent:

```text
codex exec -s workspace-write --skip-git-repo-check -C "<dir>" -o "<out>" "<prompt>"
```

- Each Codex session is fresh; the repository carries context. `resume --last` is only for an immediate one-line follow-up.
- On an active restart, reinstall the session watchdog cadence: liveness, hourly operations/leverage, adversarial audit/anti-tunnel, and entropy/consolidation.
- Nothing closes without depositing its transferable residue—constraint, harness, lemma, or negative result—into the appropriate sibling project and portfolio index.
- No new files by default; use one canonical `experiments/run_*.py` runner per artifact and config-driven variation. If it is not in both `experiments/ledger.jsonl` and `experiments/EXPERIMENTS.md`, it did not happen; negative results are permanent. The ledger is append-only.
- One idea per commit; commit messages end `Committed by Devansh`. Never use `git add -A`. Never commit `AGENTS.md`, `internal/`, `.codex_*`, `.claude_*`, or `.blackboard/`. Never put model names, providers, costs, or routing in commit messages, branch names, or PR text; evidence-bearing result files still pin the evaluated model revision as required by the evidence gates. Never edit a hash-bearing locked config or runner; successors get new filenames and a new lock.


## Hardware and process facts

Verbatim from the local operating file:

> Sustained GPU load hard-crashes this laptop (Event 6008; battery ~63% health; clock/power limits OEM-locked). Explicit user approval per GPU run; one compute job at a time; launch detached (`Start-Process`) with per-item checkpoints; short bursts survive, sustained batch generation does not; Qwen3-0.6B for local prototyping, larger models on stable/cloud hardware. After any unexplained session restart: `Get-WinEvent -FilterHashtable @{LogName='System'; Id=6008,41}`. Windows env: `PYTHONUNBUFFERED=1 PYTHONIOENCODING=utf-8`; per-run `gc.collect()` + `torch.cuda.empty_cache()`.

The completed bridge preflight was CPU fp32. Device and dtype for every other
run are authoritative only in its config and ledger row.
Qwen3-1.7B-Base is pinned to revision
`ea980cb0a6c2ae4b936e82123acc929f1cec04c1` (see `experiments/config/onewrite_recall_v1.json`).

## Canonical runners

Commands for closed runners are for reproduction only; none is a restart
instruction. The exact command and hashes for every run are in its ledger row.

| Runner | Exact command authority (ledger ids) |
|---|---|
| `experiments/run_lm_dynamics.py` | The NLM-007 ledger result rows; do not reconstruct its multi-stage commands |
| `experiments/run_coordinate.py` | `coordinate_v1_result`, `coordinate_v2_baseline`, `coordinate_v3_result` |
| `experiments/run_interchange.py` | `interchange_v1_baseline`, `interchange_v2_lock`, `interchange_v2_result` |
| `experiments/run_state_bus.py` | `state_bus_v1r1_lock`, `state_bus_v1r1_result`, `state_bus_v1r1_audit29` |
| `experiments/run_control_cost.py` | `control_cost_v1_result` |
| `experiments/run_onewrite_state.py` | `onewrite_state_v1_killed_prelock` (also the shared LM/hook machinery imported by later runners) |
| `experiments/run_onewrite_recall.py` | `onewrite_recall_v1_lock`, `onewrite_recall_v1_result`, `onewrite_recall_rung1_lock`, `onewrite_recall_rung1_result` |
| `experiments/run_oracle_actuator.py` | `oracle_actuator_rung0_lock` |
| `experiments/run_site_oracle.py` | `site_oracle_v1_lock` |
| `experiments/run_reachability.py` | `reachability_v1_lock` |
| `experiments/run_necessity_navigator.py` | Deferred; smoke command only in `navigator_smoke_r32fixes`; do not run |
| `experiments/run_necessary_register.py` | `necessary_register_v1_lock` (rung 0), `necessary_register_rung1_lock` (rung 1, config-selected `"stage": "writer"`) |
| `experiments/run_register_bridge_preflight.py` | `register_bridge_preflight_v1_lock`; do not rerun |
| `experiments/substitution_probe.py` | Shared loader (model, tokenizer, pinned revision) |
| `experiments/analyze_lexical_closeness.py`, `analyze_lm_dynamics.py`, `run_lexical_closeness.py`, `run_nlm002_vision.py`, `build_vision_cache.py`, `op_update_fixture.py` | Historical (NLM-001…007 and the toy program); their ledger rows name them |

The three most recent reproducibility commands (all completed and closed):

```text
python experiments/run_necessary_register.py --config experiments/config/necessary_register_v1.json
python experiments/run_necessary_register.py --config experiments/config/necessary_register_rung1.json
python experiments/run_register_bridge_preflight.py --config experiments/config/register_bridge_preflight_v1.json
```

## Restart procedure (written out)

This is the full sequence a returning agent or teammate follows. Nothing in it
is optional and nothing before step 5 runs a model.

**Step 0 — Orientation (no writing yet).** Do the "First 30 minutes" list
below. Confirm `git status --short` is clean and `git log -1` is at or after
`a695f8d`. Confirm no `python experiments/run_*.py` process is running.

**Step 1 — Direction dialogue with Codex, round 1 (design).** Codex is the
real CLI (command in the operating contract). Start a fresh session; do not
resume old ones. Tell Codex to read `docs/HANDOFF_2026_08_30.md`, the
`STATE.md` Current statement, `docs/STRUCTURED_NEGATIVE_2026_08_29.md`,
`experiments/run_necessary_register.py`, `experiments/run_register_bridge_preflight.py`,
`experiments/results/register_bridge_preflight_v1/run_result.json`,
`experiments/results/necessary_register_rung1/run_result.json` (and the
`writer_seed*.pt` / `consumer_seed*.pt` checkpoints), and the last ten ledger
rows; use the blackboard (`bb_list` first). Ask it to specify, as a lockable
design, the one causal test that audit #39 pre-declared — audit #39's ranked
next increments, verbatim:

1. Conduct the mandatory 2–3-round direction dialogue around the causal bridge and its stop rule.
2. Lock one residual-to-writer-centroid mapping test using training entities/templates/permutations only.
3. Evaluate injection into the frozen constructed consumer on held-out entities, templates, permutations, and paired reassigned legends.
4. Include zero, wrong-centroid, shuffled-label, embedding-derived, and destroyed-legend controls in that same run.
5. On causal success, license only cross-system causal consumption under this interface and decide whether one indirect-source generalization is warranted.
6. On failure, close the synthetic register bridge under this route; do not reopen layer, prompt, decoder, or actuator sweeps.
7. After closure or causal success, move to an orthogonal real-model task where the state is not recoverable by matching an identical explicit tag.

Ask Codex to state, for the test: the exact mapping (which layer — audit #39
found the signal depth-localized at layer 16 — which span, what linear map
from Qwen record-span residuals to the three seeds' writer centroids, fit on
training entities/templates/permutations only); the injection (the mapped
vector replaces the `<REG>` embedding of the frozen `necessary_register`
consumer, exactly as the writer's unit vector did); the held-out evaluation
(held-out entities, the two held-out templates, the held-out permutation
bank, and the paired reassigned-legend arm); the controls (zero,
wrong-centroid, shuffled-label mapping, input-embedding-derived mapping,
destroyed-legend); the pre-declared gates and statuses; the licensed
sentence for PASS and for FAIL; and the stop rule (a FAIL closes the
synthetic register bridge under this route with no repair).

**Step 2 — Direction dialogue, round 2 (challenge).** Push back on round 1 in
writing: the strongest alternative explanation audit #39 recorded, verbatim,
is —

The strongest alternative explanation is a local dictionary-lookup mechanism: the record tag attends to its identical legend occurrence and inherits the nearby numeral. That is genuine contextual binding, but it may be prompt-local rather than a reusable latent state.
Useful alternatives or embedded controls are:
- Include the paired reassigned-legend arm in the causal bridge test.
- Compare residual-to-centroid mapping against input-embedding, wrong-centroid, zero, and shuffled-label mappings.
- Require held-out entities, templates, and permutations in the causal consumer test.
- Treat legend-occurrence mapping as an easier positive reference, not the headline route.
- After this line resolves, test an orthogonal real-model source where the state is indirect and cannot be obtained by matching an identical explicit tag.
- Do not run standalone attention localization or further decoder sweeps before the causal discriminator; the ratio no longer permits another measurement-only detour.

Ask Codex to show how the design distinguishes consumer-following from
these alternatives, what the simplest confound is that could explain every
row, and what a PASS would and would not license. Change the design if it
cannot answer.

**Step 3 — Direction dialogue, round 3 (lock ruling).** Bring the implemented
runner and config back to Codex for a lock review (as rounds 27–28 did for
the preflight). Codex rules "ready" or lists exact edits. Apply them
verbatim. No scientific change after the ruling.

**Step 4 — Lock.** New config file (never edit a hash-bearing one), runner
changes only through a config-selected stage or a new `run_*.py`; smoke
(`--smoke`) for mechanics only; append a `lock` row to
`experiments/ledger.jsonl` with config and runner sha256, the gates, the
statuses, the licensed wording, and the stop rule; commit by explicit path
(`Committed by Devansh`); push.

**Step 5 — Run once.** CPU only, one process, detached, within the config's
wall clock. No GPU without Devansh's approval.

**Step 6 — Record and audit once.** Append the `result` row (metrics, artifact
paths, hashes); commit the result files; launch a FRESH, unprimed Codex
auditor (no prior context; both directions; must answer "should this
continue" and name the strongest alternative); adopt its language verbatim
into `STATE.md`, `README.md`, `NOTEBOOK.md`, `experiments/EXPERIMENTS.md`,
and a ledger `audit` row. Raw runner status strings never outrank the audit.

**Step 7 — Branch.** On a causal PASS: license only cross-system causal
consumption under this interface and open a new direction dialogue on one
indirect-source real-model task (the state must not be recoverable by
matching an identical explicit tag). On FAIL: close the synthetic register
bridge under this route; do not reopen layer, prompt, decoder, or actuator
sweeps; return to a direction dialogue. Either way, report the ratio
(24 measurement/governance : 7 build at the pause — above the 2:1 warning;
the next active round must build the causal discriminator or close the
line).

## Repository map

- `README.md`: public front door.
- `docs/HANDOFF_2026_08_30.md` (this file): pause and restart authority.
- `STATE.md`: canonical scientific wording and never-say lists; the "Current statement" at the top governs, everything below it is the historical record.
- `docs/STRUCTURED_NEGATIVE_2026_08_29.md`: the audited two-day synthesis of the frozen-model line.
- `experiments/ledger.jsonl`: append-only run, hash, command, result, and audit provenance (one JSON object per line; where present, `event` records stages including lock, result, audit, preflight, design_gate, and ops_heartbeat; older rows may omit it).
- `experiments/EXPERIMENTS.md`: detailed experiment record; older queue language is historical.
- `NOTEBOOK.md`: reverse-chronological dialogue and audit record (verbatim Codex rounds and audits); not claim authority over `STATE.md`.
- `experiments/config/*.json`: immutable contracts for completed runs.
- Recent results commonly use `experiments/results/<run>/run_result.json` or `result.json`; historical runs also use `baseline_result.json`, `full_result.json`, `analysis*.json`, or `verdict.json`. The ledger row's `data_ref` is authoritative. Raw outcomes' status strings may be narrowed by audit wording. Raw rows (`*_rows*.json[.gz]`) and small checkpoints are committed where they were retained.
- `legacy/`: the closed previous program, not an active queue.
- Local-only (git-ignored; absent from a fresh clone): `AGENTS.md`, `.codex_*` audit/direction transcripts (full transcripts are local-only; audit-adopted scientific wording is retained in `STATE.md`, `NOTEBOOK.md`, and the ledger, but not every transcript is guaranteed to be reproduced byte-for-byte), `.blackboard/`, run logs, `experiments/results/register_bridge_preflight_v1/run_features.npz` (float32 features for recomputation; hash in the ledger result row).

## First 30 minutes for a fresh reader

```md
1. Run `bb_list`; reuse the existing direction/audit board if present. In a fresh clone with no `.blackboard/`, create one restart/handoff board and seed it from this handoff, the `STATE.md` Current statement, and the final ledger rows.
2. Read README → this handoff → STATE Current statement → the structured negative.
3. Read the final five ledger rows, audit #39's adopted block, and the preflight result summary.
4. Run `git status --short` and record HEAD; do not edit hash-bearing configs or runners.
5. Confirm that no experiment process is active.
6. Classify every proposed action against audit #39's restart rule.
7. State expectation, outcome branches, simplest confound, and the current ratio before proposing a run.
8. If work resumes, begin a fresh 2–3-round direction dialogue; do not resume an old repair ladder.
```

## Newcomer traps

- Raw status strings are not final scientific claims. Audit #37 narrowed rung 0; audit #38 narrowed rung 1; audit #39 governs the preflight.
- `state_bus_v1r1`'s displayed status omits one registered failure because of control flow; use audit #29.
- The oracle-actuator result's initial "two codes worked" interpretation was corrected: code 0 reproduced the cue prior.
- `site_oracle_v1`'s reported cap-active rate was numerical boundary rounding, not meaningful clipping.
- `reachability_v1` does not establish two or three reachable dimensions.
- The tracked navigator `smoke_result.json` is not the 2,000-step smoke cited in `STATE.md`; `NOTEBOOK.md` records that the live file was overwritten by a later 300-step invalid smoke. Cite the ledger row, never that file, for the 2,000-step numbers.
- `legacy/` is historical and closed; its older NEXT/queue language is not authorization.
- `.codex_*`, `AGENTS.md`, `.blackboard/`, logs, and feature dumps are git-ignored. A fresh clone will not contain them.
- Many `NOTEBOOK.md` links are absolute paths tied to the original laptop.
- Hash-bearing completed configs and runners are evidence. Editing them destroys reproducibility; successors need new filenames/configs and a new lock.
- The final closeout is complete only when `git status --short` is clean or every remaining local-only evidentiary artifact is listed above.
