# Diffusion Theory Claim Ledger

This ledger keeps the mathematical theory honest. It maps each theory claim to
its current evidence, assumptions, falsifiers, and next proof obligation.

Status meanings:

- `validated-local`: supported by current generated artifacts and fresh CUDA or
  deterministic validation in this repo.
- `supported-conditional`: mathematically coherent and supported by local
  evidence, but conditional on the current abstraction or task slice.
- `hypothesis`: plausible direction that still needs broader evidence.
- `boundary`: negative or cautionary result that limits the claim.

## Claim Ledger

| ID | Status | Assertion | Current Evidence | Assumptions | Falsifier | Next Proof Obligation |
| --- | --- | --- | --- | --- | --- | --- |
| T1 | supported-conditional | A diffusion trajectory is a better latent-reasoning object than a final string because intermediate states are editable. | `DIFFUSION_REASONING_GEOMETRY_THEORY.md` Proposition 1; `DIFFUSION_DENOISE_PHASE_GEOMETRY.md`; public repair lift from `0.412277` greedy/fixed to `0.531116` latent repair. | The repair operator can remask verifier-relevant spans and the backend exposes usable denoise history. | A comparable AR rewrite/repair system with the same cost and verifier inputs matches the lift without diffusion-state access. | Run an equal-cost AR rewrite baseline with the same verifier spans and cost accounting. |
| T2 | validated-local | The current public MoE result is not random perturbation; it is selected latent repair over denoise trajectories. | `DIFFUSION_PUBLIC_BENCHMARK.md`: greedy/fixed `0.412277`, random `0.372125`, latent repair `0.531116`. | The public three-arm ledger is the right comparison for the current local GPU budget. | Random perturbation with the same cost and no repair matches or beats selected repair on the same task set. | Add another cheap mixed slice and preserve the same arm discipline. |
| T3 | validated-local | Phase-window caps create a piecewise-constant score/cost frontier. | `DIFFUSION_PHASE_WINDOW_BUDGET_MAP.md`: cap `9`, `10-19`, `20-30`, `31+` regimes with zero confirmation mismatches. | First repairable denoise skeleton onset is the only active cap transition in the current operator. | A fresh CUDA cap inside a predicted plateau changes score, selected tasks, or cost and therefore fails the plateau forecast. | Re-run the same cap-map derivation on the next benchmark slice. |
| T4 | validated-local | Repair spending should be optimized by marginal value, not by repairing every repairable state. | `DIFFUSION_BUDGET_POLICY_LOSS.md`; at lambda `0.18`, task-gated oracle keeps `plan_004`, `plan_006`, `plan_007`; `DIFFUSION_BUDGET_VALUE_PROXY_AUDIT.md` and `DIFFUSION_COMPOSITE_SELECTOR_RUNNER_POLICY.md` confirm `0.508705` at `2.375000x`. | Linear compute penalty is a useful local objective and repair spends are additive enough on this slice. | All-repairable spending dominates the value-proxy policy under the same lambda and cost definition. | Test whether marginal repair value remains predictive on new planning tasks. |
| T5 | validated-local | The current profitable repair set is separable by label-free source-quality and prompt-gap geometry. | `DIFFUSION_REPAIR_VALUE_GEOMETRY.md`: runner proxy has zero regret; in-band source-quality separation gap is `0.022857`; first excluded low-quality gap is `10`. | Current labels are derived from the cost-aware loss targets; thresholds are slice-local. | A held-out task with the same feature geometry is misclassified and erases utility gain. | Replace threshold reuse with a small learned predictor over the same feature family. |
| T6 | supported-conditional | Judge, verifier, and anchor channels must be accounted for separately from frozen-model information. | Exact-answer proposal ablations; claim evidence map separates raw generations, scores, reports, and diagnostics; `DIFFUSION_REASONING_GEOMETRY_THEORY.md` Proposition 5. | Selection changes output distributions even with frozen weights. | A promoted claim fails if it attributes gains to the model alone while relying on hidden post-hoc labels or unavailable judge information. | Add a standardized information-channel table to every promoted theory claim. |
| T7 | boundary | Denoise history is selector evidence before it is trusted as a replacement source. | `DIFFUSION_PHASE_SOURCE_THRESHOLD_SWEEP.md`: loose `0.90/0.90/0.90` source promotion regresses to `0.524554` while strict/final-preserving stays `0.531116`. | Weak phase history can be repairable but not source-safe. | A learned source selector consistently improves over final-source repair without extra cost or regressions. | Train/test phase-source trust loss on additional history/final source decisions. |
| T8 | supported-conditional | Safe repair requires a retention constraint. | `DIFFUSION_ANCHOR_RETENTION_LOSS.md`, `DIFFUSION_REALIZATION_QUALITY.md`, and phase-source regressions show source replacement and anchor realization can damage useful constraints. | Benchmark scores may not fully capture every destroyed constraint. | Unconstrained repair transfers across new tasks without retention failures or hidden regressions. | Report retention loss beside every new repair operator and correlate it with transfer failures. |
| T9 | hypothesis | Verifier-aligned trajectory loss is a better training objective than final-token likelihood for this use case. | Current evidence shows verifier features predict repair spend and source trust; no model has been trained on this loss yet. | Verifier features are learnable from accessible denoise states and transfer across tasks. | Training on verifier-aligned trajectory loss fails to improve repair value or source trust over current hand-coded gates. | Build a small supervised selector or scorer from generated target rows before training a model. |
| T10 | hypothesis | Energy-aware denoise control can become a general reasoning controller. | Phase-budget ladder, value proxy, and repair-value geometry show cost-aware decisions improve utility on the current slice. | Relative generation count is a meaningful proxy for local GPU cost. | The policy wins only because of this exact task mix or scoring scale and fails on any broadened slice. | Add a second lean mixed suite and preserve cost-normalized reporting. |
| T11 | supported-conditional | The next error function should decompose repair value, source trust, retention, and anchor realization instead of using a single repairability label. | `DIFFUSION_ERROR_FUNCTION_GEOMETRY.md`: raw lift shrinks from five positive repair targets to three cost-profitable targets at lambda `0.18`; earliest repairable step `10` has mixed value; naive repairable-history source trust creates four false positives; `DIFFUSION_DECOMPOSED_SELECTOR_AUDIT.md` now scores all four terms. | Current evidence is from the lean mixed diffusion target rows and hand-scored source-choice, retention, and realization audits. | A single repairability classifier matches or beats the decomposed loss family on held-out value/source/retention/realization decisions. | Train/test a decomposed selector against a single-label repairability selector on the next lean mixed slice. |
| T12 | validated-local | A single repairability-label controller is locally dominated by the decomposed value/source/retention/realization selector. | `DIFFUSION_DECOMPOSED_SELECTOR_AUDIT.md`: single repairability has composite shortfall `3.053730`, value confusion `3/3/2/0`, source confusion `1/4/0/0`, retention error `1.566063`, and realization error `0.573292`; decomposed value/source has composite shortfall `0.186127`, zero value regret, zero source error, zero retention error, and realization error `0.186127`. | Composite shortfall is a useful local diagnostic even though the four loss terms still need transfer calibration. | A single-label repairability controller matches or beats decomposed value/source/retention/realization on the same target rows under the declared scoring rule. | Test the same comparison on a held-out lean mixed slice and learn the four-term selector weights. |
| T13 | validated-local | The four-term composite loss now has a concrete supervised target surface. | `DIFFUSION_COMPOSITE_SELECTOR_TARGETS.md`: emits eight task-level rows for spend/source/retention heads and seven realization-policy rows for the compact-anchor head; `diffusion_composite_selector_targets.jsonl` is the machine-readable training surface. | The current target rows are local and derived from generated audits rather than a held-out benchmark. | The target builder fails to recover selected spend/source/realization decisions or omits one of the four declared loss heads. | Train a small selector on these rows, then rebuild the target surface on a held-out lean mixed slice. |
| T14 | validated-local | A tiny interpretable four-head selector can fit the current composite target surface exactly. | `DIFFUSION_COMPOSITE_SELECTOR_FIT.md`: zero training error with spend rule `first_repairable_gap_le_9_source_quality_le_0p301429`, source rule `retention_safe_history`, retention rule `classification_safe_history_anchor`, and realization rule `min_realization_policy_error`. | Exact local fit is not evidence of transfer; this is a baseline floor for learned selectors. | The same target rows produce a training error under the fitted rules or a simpler single-label controller matches all four heads. | Test this fitted selector and a learned selector on a held-out lean mixed target surface. |
| T15 | validated-local | The decomposed selector heads retain most of their advantage under leave-one-task-out evaluation. | `DIFFUSION_SELECTOR_HOLDOUT_EVAL.md`: hiding each task row and refitting on the other seven produces `4` decomposed errors over `21` scored labels, versus `12` errors for a single first-repairable repairability controller. | Leave-one-task-out on the same target surface is stronger than exact fit but weaker than a fresh GPU benchmark slice. | A single repairability controller matches or beats the decomposed heads under the same leave-one-task-out scoring rule. | Run the same holdout evaluator on the next independently generated lean mixed target surface. |
| T16 | validated-local | The four-head selector is now executable in the diffusion benchmark runner. | `DIFFUSION_COMPOSITE_SELECTOR_RUNNER_POLICY.md`: documents `--repair-spend-trigger decomposed_four_head_selector`, full repair-spend head provenance, test coverage, and fresh CUDA run `diffusion-62476b492c9e592c` scoring `0.508705` at `2.375000x`. | The implemented trigger wires the fitted spend head first; source, retention, and realization heads remain policy diagnostics around the existing phase/final repair path. | The runner rejects the trigger, fails to emit selector diagnostics, or matches only the old unnamed value-proxy path without four-head provenance. | Test the controller on an independently generated lean mixed target surface without reusing the current fitted thresholds. |
| T17 | boundary | The fitted spend head transfers as a repair-availability filter, but larger slices show availability must be trajectory-relative. | `DIFFUSION_INDEPENDENT_SPEND_TRANSFER.md` and `DIFFUSION_INDEPENDENT_SPEND_TRANSFER_V2.md`: decomposed spend has `0` errors on four-row and eight-row transfer slices; `DIFFUSION_INDEPENDENT_SPEND_TRANSFER_V3.md`: on 16 planning rows, single repairability has `4` errors, older decomposed spend has `1`, and trajectory-relative decomposed spend has `0`. | The independent slices still evaluate only the availability head; source, retention, realization, and cost-adjusted promotion remain separate. | A larger independent slice regresses because the trajectory-relative term drops positive repair-availability cases or admits no-lift repairs at the same or higher rate than single repairability. | Test trajectory-relative spend on another fresh mixed planning slice and add a learned replacement for the hand threshold. |
| T18 | boundary | A strict source-task floor is a cost-conservative margin policy, not the best repair-availability predictor. | `DIFFUSION_SPEND_TRANSFER_RULE_FIT.md` and `DIFFUSION_SPEND_TRANSFER_RULE_FIT_V2.md`: `current_decomposed_spend` has zero errors, while source-task floors above `0.295357`, including the earlier `0.3075` floor, create a false negative by skipping positive low-margin repair `plan_012`; run `diffusion-f50e82f88f59111b` verifies that strict floor in the live runner. | Repair availability and cost-adjusted promotion value are distinct labels; the current promotion margin can hide a positive repair candidate. | A larger independent slice shows a stricter source-task floor improves cost-adjusted utility without dropping positive repair-availability cases that matter for the chosen utility threshold. | Train separate predictors for repair availability and cost-adjusted promotion value. |
| T19 | validated-local | Repair availability and repair promotion value are separable heads on the expanded transfer slice. | `DIFFUSION_TRANSFER_PROMOTION_VALUE.md`: with the corrected spend gate, planning-quality seed-realization selection generates but does not select the `plan_012` repair and leaves `0.002500` mean headroom; inherited planning-state selection selects it, improves repair-covered planning score to `0.350938`, and leaves `0.000000` oracle headroom. | The inherited planning-state selector is a local proxy for promotion value, not yet a trained promotion head. | A larger transfer slice shows inherited planning-state selection selects harmful repairs or fails to recover available positive repairs as often as the planning-quality selector. | Train a promotion-value head and compare it with inherited planning-state selection on expanded transfer rows. |
| T20 | boundary | The next diffusion error function should be a proof object with separate availability, promotion, source-trust, retention, and cost terms. | `DIFFUSION_REASONING_PROOF_OBJECT.md`: six heads, 60 target rows, 6 measured errors, explicit information channels, falsifiers, and next GPU validations; the availability head is marked `boundary` because v7 shows calibrated pre-repair availability still misses spend value, while `DIFFUSION_CANDIDATE_PROMOTION_TARGETS_V7.md` gives the post-repair promotion head zero local errors. | Generated target rows are adequate local witnesses for where information is gained, lost, retained, or made too expensive, but fixed thresholds are not assumed transfer-stable. | A learned single-objective repairability scorer matches or beats the decomposed proof-object terms on fresh GPU slices without extra hidden labels. | Test a learned spend gate offline against v5/v6/v7 rows while keeping `candidate_aware_promotion_v1` fixed. |
| T21 | boundary | Repair availability is relative to the selected trajectory state, but selected-trajectory geometry alone does not solve promotion. | `DIFFUSION_INDEPENDENT_SPEND_TRANSFER_V3.md`: under corrected repair-only labels, trajectory-relative availability has `1` error over 16 planning rows; `DIFFUSION_AVAILABILITY_PREDICTOR_FIT.md` shows the best corrected v3 pre-repair rule still has `1` error. | The selected trajectory score is a legitimate available information channel before repair spending because trajectory selection has already happened, but it is not a substitute for post-repair promotion evidence. | A fresh slice fails this split if selected-trajectory delta plus post-repair diagnostics no longer improves over single repairability or all-repairable spending. | Test selected-trajectory geometry as one spend-gate feature and `candidate_aware_promotion_v1` as the separate post-repair head. |
| T22 | boundary | A fixed absolute source-quality ceiling is not a transfer-stable learned availability rule. | `DIFFUSION_INDEPENDENT_SPEND_TRANSFER_V4.md`: v4 has profitable repairs on `plan_026`, `plan_028`, and `plan_031`; `learned_availability_predictor_v1` blocks all three because their source-quality values exceed the v3 ceiling. CUDA run `diffusion-865e5acb0ee73e8a` executes the learned trigger and runs zero repairs. | The v4 labels use the same all-repairable oracle-lift method as v3, so this is a rule-transfer failure rather than a label-schema change. | A slice-relative or calibrated source-quality model recovers the v4 profitable rows without re-admitting the v3 no-lift false positives. | Train a calibrated availability model that uses source quality relative to task/slice geometry, then evaluate it on another fresh planning slice without retuning. |
| T23 | boundary | Calibrated pre-repair availability is useful but not sufficient for repair promotion. | `DIFFUSION_INDEPENDENT_SPEND_TRANSFER_V6.md`: calibrated availability has `4` spend errors and misses positive repairs on `plan_046` and `plan_048`; `DIFFUSION_CANDIDATE_PROMOTION_TARGETS_V6.md` shows `candidate_aware_promotion_v1` has `0` promotion errors over eight generated repair candidates. CUDA run `diffusion-b6d8fd700b3a267f` still beats fixed by `0.047036` and random by `0.086268`, but repairable-denoise spending plus candidate-aware promotion (`diffusion-ae7a4edd5c22ca20`) is stronger at `+0.086696` versus fixed and `+0.125929` versus random with zero oracle headroom. `DIFFUSION_SPEND_POLICY_DECISION.md` shows the stronger policy costs `0.375000` more relative extra generations per task than calibrated spend and buys `0.105762` incremental lift per added generation. | The v6 labels distinguish spend-gate failure from promotion success: the post-repair selector generalizes, but the calibrated availability trigger blocks valuable candidates before they exist. | A later slice fails this claim if repairable-denoise spending plus candidate-aware promotion loses to calibrated spend at matched cost or promotes harmful generated repairs. | Train a spend gate that preserves v6 positives without reintroducing v5/v6 no-lift rows, then test it on a fresh slice. |
| T24 | boundary | The no-retuning v7 slice confirms promotion transfer but exposes the cost of broad repairable-denoise spending. | `DIFFUSION_INDEPENDENT_SPEND_TRANSFER_V7.md`: only `plan_054` and `plan_056` are profitable repair rows, while single repairability selects all eight repairable rows and calibrated availability makes `5` errors. `DIFFUSION_CANDIDATE_PROMOTION_TARGETS_V7.md` shows `candidate_aware_promotion_v1` has `0` errors and selects exactly the two positive generated repairs. CUDA run `diffusion-711ea5fcfd8c07e5` beats fixed by `+0.023036` and random by `+0.082063` on repair-covered tasks, but leaves `0.016875` oracle headroom. | The v7 run freezes the promotion head and task slice before scoring, so the result is a transfer check rather than a retuned threshold win. | A fresh v8 slice fails this boundary if a pre-repair spend gate preserves positives and dominates repairable-denoise spending at matched cost without promotion errors. | Train and evaluate a spend gate on combined v5/v6/v7 labels, then run only the best frozen gate on v8. |
| T25 | boundary | Span tomography improves probe reliability, but the local distinct-retention Stage 1 threshold is not transfer-stable. | `DIFFUSION_COUNTERFACTUAL_SPAN_VALIDATED_PROBE_STAGE1_GATE_V4.md`: the local 12-row semantic-valid fit has zero errors with `valid_measured_distinct_retention_risk_visibility_ge_0p927195`. `DIFFUSION_COUNTERFACTUAL_SPAN_DISTINCT_RETENTION_RULE_V4_FRESH_PLANNING.md` and `DIFFUSION_COUNTERFACTUAL_SPAN_VALIDATED_PROBE_TRANSFER_MATRIX_V4.md`: the same frozen rule has `3` errors on independent `plan_001`-`plan_008` rows, with false positives on `plan_002` and `plan_003` and a false negative on `plan_004`; the fresh probe text is 8/8 semantic-valid. | The train and transfer slices share the same span-v4 probe policy and semantic-validity requirement, so the failure is a controller-feature transfer failure rather than a malformed-probe failure. | A learned or normalized value-of-information feature evaluated without retuning has zero or utility-dominant errors on both the named counterexamples and a fresh planning slice. | Build a cross-slice probe-value learner that trains on named counterexamples, evaluates on `plan_001`-`plan_008`, and reports strict transfer before any live spend promotion. |
| T26 | boundary | The transfer-screened gap/span conjunction was slice-shaped and is not a transfer-stable Stage 1 probe gate. | `DIFFUSION_COUNTERFACTUAL_SPAN_VALIDATED_PROBE_CONJUNCTION_TRANSFER_V4.md`: the screened rule `measured_expected_gap_visibility_gain >= 0.666667` and `measured_expected_span_evidence_gain <= 0.600000` had `1` named-surface error and `0` errors on `plan_001`-`plan_008`. `DIFFUSION_COUNTERFACTUAL_SPAN_GAP_SPAN_RULE_V4_TRANSFER_V3_PLANNING.md`: frozen on `plan_017`-`plan_024`, the same rule has `3` errors, with false positives on `plan_020` and `plan_023` and a false negative on `plan_018`; probe text remains 8/8 semantic-valid. | The failed next-slice test uses the same span-v4 probe policy and no-retuning rule, so the failure is not a malformed-probe artifact. | A richer probe-signature model fails if it still cannot beat the rejected hand rules on two non-overlapping transfer slices without retuning. | Build a learned or multi-signature probe value model that treats gap, span, retention, overlap, source geometry, and task slice as features, then evaluate on held-out planning rows before any live spend promotion. |
| T27 | boundary | Richer nearest-prototype span probe signatures preserve positives but do not solve no-lift specificity. | `DIFFUSION_COUNTERFACTUAL_SPAN_PROBE_SIGNATURE_MODEL_V4.md`: across 28 rows and 3 span-v4 slices, the signature model preserves all 15 positive rows in sample and under leave-one-slice-out, but still has `11` leave-one-slice-out errors, all false positives; covered positive lift is `1.145500`. | The audit reuses the frozen span-v4 target sheets and scores each slice with a model fit only on the other slices, so the result tests cross-slice signature transfer rather than a retuned local threshold. | A stricter signature model fails this boundary if it preserves all positives while reducing leave-one-slice-out false positives below the rejected hand-rule baselines only by retuning on the held-out slice or by using labels not available before spending. | Train a cost-aware or signature-specific model that penalizes no-lift selections, then evaluate leave-one-slice-out and a fresh GPU slice before promotion. |
| T28 | boundary | Cost-penalizing the span signature frontier exposes the recall/specificity failure rather than solving it. | `DIFFUSION_COUNTERFACTUAL_SPAN_PROBE_SIGNATURE_UTILITY_FRONTIER_V4.md`: over 30 thresholds and 6 selection penalties, penalties through `0.020000` retain the broad 26-row policy with `11` false positives; at `0.050000`, the best utility threshold selects only `3` rows and misses `13` positives. | The frontier is computed only from leave-one-slice-out signature scores, so it tests whether a frozen scalar score can be cost-calibrated without reusing held-out labels as a feature. | A cost-aware scalar signature gate fails this boundary if it cannot reduce no-lift false positives at practical penalties without dropping most positive rows or if the tradeoff disappears on a fresh slice. | Train a separate no-lift head or direct realized-lift objective over measured probe signatures, then freeze it before another GPU slice. |
| T29 | boundary | A threshold-fragment no-lift veto is not a transfer-stable second-stage controller. | `DIFFUSION_COUNTERFACTUAL_SPAN_PROBE_NO_LIFT_VETO_V4.md`: leave-slice-out search over `73,154` one- and two-condition veto rules lowers false positives from `11` to `8`, but creates `5` false negatives and lowers practical-penalty utility from `0.625500` to `0.289429`. | The veto is trained only on selected rows from the non-held-out slices, so the reported failure is a two-stage controller transfer failure rather than a local-rule search failure. | A threshold-fragment veto fails this boundary if its specificity gain still costs positive rows or lower utility under leave-slice-out transfer at the same selection penalty. | Train a signed realized-lift predictor over measured probe signatures and evaluate utility directly before any fresh GPU promotion test. |
| T30 | boundary | Direct signed-value tomography improves utility accounting but is not yet a promotable spend controller. | `DIFFUSION_COUNTERFACTUAL_SPAN_PROBE_SIGNED_VALUE_V4.md`: the selected `signed_value_knn_k8_all` head preserves all 15 positives, reduces false positives from `11` to `9`, and improves strict signed utility from `0.498571` to `0.582500`; it still misses the architecture promotion bar of `0.625500`, and the `plan_017`-`plan_024` slice keeps `6` false positives with only `0.001429` signed utility. | The head predicts realized lift minus cost directly from leave-one-slice-out probe signatures, so negative realized lift is charged rather than hidden inside a binary false-positive count. | A signed-value controller fails this boundary if it cannot exceed `0.625500` utility, preserve zero false negatives, and reduce false positives below `9` under leave-one-slice-out transfer. | Add negative-control feature-family ablations and stronger signed-value features, then freeze the improved head before any fresh GPU promotion test. |
| T31 | boundary | Signed-value utility depends on the distributed probe signature rather than one removable feature family. | `DIFFUSION_COUNTERFACTUAL_SPAN_PROBE_SIGNED_VALUE_CONTROLS_V4.md`: all `4/4` matched-k controls degrade relative to `signed_value_knn_k8_all`, with utility deltas from `-0.151071` to `-0.041786`; all `4/4` best-withheld controls also remain below the full model. | The controls withhold text-fidelity, source, gap/span, and retention feature families from the same leave-one-slice-out signed-value sweep, so the test targets feature-family dependence rather than a new task slice. | A distributed signature claim fails if any withheld-family control matches or exceeds the full model while preserving the same zero-false-negative transfer profile. | Add stronger signed-value features focused on the weak `plan_017`-`plan_024` slice before considering a frozen fresh GPU test. |
| T32 | boundary | The remaining signed-value failure is a weak-slice cohort-calibration problem, not a solved GPU-ready controller. | `DIFFUSION_COUNTERFACTUAL_SPAN_PROBE_SIGNED_VALUE_WEAK_SLICE_V4.md`: on `plan_017`-`plan_024`, `signed_value_knn_k8_all` selects all `8` rows, but only `2` are positive; the six false positives leave just `0.001429` signed utility and a selected positive rate of `0.250000`. | The weak-slice diagnosis replays the same frozen leave-one-slice-out signed-value head and uses only measured span-v4 probe signatures plus realized signed value for audit labels. | A cohort-calibration feature fails this boundary if it cannot lower weak-slice no-lift selections without creating false negatives or lowering overall leave-one-slice-out signed utility below `0.582500`. | Add a cohort-density, uncertainty, or neighbor-disagreement signed-value feature and rerun M1/M2 before any frozen fresh GPU test. |
| T33 | boundary | Neighbor-risk calibration improves global signed utility but still does not solve weak-slice cohort calibration. | `DIFFUSION_COUNTERFACTUAL_SPAN_PROBE_COHORT_RISK_V4.md`: the selected `cohort_risk_k13_std0_negfrac0p03_marginn0p01` head reaches `0.685500` signed utility with `0` false negatives and `8` false positives, but its weak-slice summary is unchanged at `8` selected rows, `6` false positives, and `0.001429` signed utility. | The sweep uses only train-slice nearest-neighbor signed-value statistics, so negative-neighbor fraction is available during leave-one-slice-out scoring; the weak-slice check remains a held-out audit for this candidate. | A risk-calibrated cohort head fails this boundary if it clears global utility only by preserving the known weak-slice no-lift cohort or by using held-out labels in the scoring feature. | Add a stronger slice-local density or row-text fidelity feature that reduces weak-slice false positives below `6` while preserving zero false negatives, then rerun M1/M2. |
| T34 | supported-conditional | The weak-slice signed-value blocker needs a trajectory-relative source channel, not only richer probe text. | `DIFFUSION_COUNTERFACTUAL_SPAN_PROBE_TRAJECTORY_RELATIVE_GATE_V4.md`: adding the frozen trajectory-relative spend channel to the cohort-risk head raises signed utility to `0.805500`, preserves `0` false negatives, reduces false positives to `2`, and reduces weak-slice false positives from `6` to `0`. | The trajectory-relative feature is an allowed pre-spend information channel because it compares the repair source with the selected trajectory state, but this result is no longer a pure span-probe signature controller. | The composite claim fails if withholding or perturbing the trajectory-relative channel preserves the same utility and weak-slice gains, or if a fresh slice shows the channel blocks positive repairs. | Add trajectory-channel negative controls and only then freeze the composite for a fresh GPU slice. |
| T35 | supported-conditional | The trajectory-relative channel is load-bearing for the composite signed-value gate. | `DIFFUSION_COUNTERFACTUAL_SPAN_PROBE_TRAJECTORY_RELATIVE_CONTROLS_V4.md`: all `4/4` controls degrade relative to the true composite; no-channel falls to `0.685500` utility with `6` weak-slice false positives, delta-only leaves `5` weak-slice false positives, inverted creates `2` false negatives, and rotated creates `2` false negatives. | The controls reuse the same frozen cohort-risk rows and perturb only trajectory-channel semantics, so the audit isolates source-vs-trajectory information rather than rerunning GPU generation. | A trajectory-relative channel claim fails if a no-channel, delta-only, inverted, rotated, or fresh-slice control matches the true composite while preserving zero false negatives and zero weak-slice false positives. | Run one fresh GPU slice with the exact composite controller frozen and task IDs fixed before generation. |
| T36 | boundary | The composite span-probe gate is frozen for a fresh v10 proof-obligation slice, but is not yet a live spend trigger. | `DIFFUSION_COUNTERFACTUAL_SPAN_PROBE_COMPOSITE_FREEZE_V4.md`: freezes `lean_gpu_mixed_transfer_v10` over new `plan_073`-`plan_080` rows plus `math_009`, `sym_007`, and `sci_002`; the manifest records the measured span-probe pass, all-repairable label pass, replay inputs, and promotion gates before GPU outputs exist. | The composite needs measured span-probe features and trajectory-relative source evidence, so the next run is a locked replay validation rather than direct live-trigger promotion. | The freeze fails if the v10 task IDs overlap prior span rows, if the GPU commands are changed after labels exist, if replay misses any positive repair, or if probe cost erases the repair utility. | Run the frozen v10 measurement and label passes, replay the composite without retuning, then either implement a live trigger or keep the gate diagnostic-only. |
| T37 | boundary | The v10 GPU replay supports measured-probe usefulness but rejects composite promotion. | `DIFFUSION_COUNTERFACTUAL_SPAN_PROBE_COMPOSITE_V10_REPLAY.md`: the frozen replay selects all `8` planning rows, preserves `0` false negatives over `5` selected-repair positives, but admits `3` false positives (`plan_074`, `plan_075`, `plan_078`) and reaches only `0.428214` signed utility versus the frozen `0.625500` promotion bar. | The v10 slice is a real fresh GPU replay using committed measurement and label artifacts, but every planning row has zero source-vs-trajectory delta, so it does not stress the trajectory-relative veto that fixed the weak slice offline. | The composite promotion claim fails if fresh replay remains below the utility bar or cannot reduce no-lift rows without missing positives; this v10 run is one such failure for promotion. | Build a live/replay gate that uses measurement cost and cohort specificity, then test it on a slice where source and selected trajectory differ. |
| T38 | boundary | The trajectory-relative veto can remove a no-lift v10 row, but it is not enough for promotion. | `DIFFUSION_COUNTERFACTUAL_SPAN_PROBE_COMPOSITE_V10_FIXED_SOURCE_REPLAY.md`: the fixed-source replay creates one negative source-vs-trajectory row and blocks no-lift `plan_074`, improving false positives from `3` to `2` and utility from `0.428214` to `0.448214`, while preserving `0` false negatives. | This stress replay reuses the committed v10 label artifact and changes only the pre-spend source policy to fixed source, so it tests the trajectory channel rather than a new task slice. | The trajectory-veto claim fails if negative-delta rows are positive repairs or if blocking them does not improve no-lift specificity; promotion still fails here because `plan_075` and `plan_078` remain false positives. | Add an additional pre-spend no-lift specificity feature or cost-aware measurement term, then replay against v10 and a source-divergent fresh slice. |
| T39 | boundary | A measured probe-value floor is the first v10 no-lift specificity hypothesis, not transfer evidence. | `DIFFUSION_COUNTERFACTUAL_SPAN_PROBE_V10_NO_LIFT_SPECIFICITY.md`: the post-label diagnostic rule `measured_probe_value_prediction >= 0.028915` removes the remaining fixed-source false positives (`plan_075`, `plan_078`), preserves `0` false negatives over `5` selected-repair positives, and reaches the max possible v10 selected-repair utility of `0.488214` under the `0.020000` selection penalty. | This floor is fit after seeing v10 labels and uses the committed fixed-source measurement/replay rows only; it is a diagnostic frontier, not a live trigger or promoted controller. | The measured-value floor fails if a frozen source-divergent fresh slice misses positives, no longer removes no-lift rows, or loses its utility advantage after probe measurement cost is charged. | Run a frozen measured probe-value floor test on a fresh source-divergent slice and report utility with measurement-cost accounting. |
| T40 | boundary | The measured probe-value floor is frozen for v11, but no fresh transfer result exists yet. | `DIFFUSION_COUNTERFACTUAL_SPAN_PROBE_VALUE_FLOOR_V11_FREEZE.md`: freezes `lean_gpu_mixed_transfer_v11` over new `plan_081`-`plan_088` rows plus `math_009`, `sym_007`, and `sci_002`; the manifest locks threshold `0.02891517987715706`, fixed source policy, measurement and label commands, zero v10 fit-row overlap, required source divergence, and measurement-cost replay gates before v11 labels exist. | The v11 prompts and commands are committed before GPU generation, but the freeze only proves no-leak protocol discipline; it does not prove the measured-value floor transfers. | The freeze fails if v11 task IDs overlap fit rows, if the commands change after labels exist, if no source-divergent rows appear, or if the replay misses positives or admits no-lift rows after measurement cost. | Run the frozen v11 measurement and label passes, replay the value floor with declared ablations, and update this ledger whether the result transfers or fails. |
| T41 | boundary | The v11 measurement pass did not satisfy the source-divergent test gate. | `DIFFUSION_COUNTERFACTUAL_SPAN_PROBE_VALUE_FLOOR_V11_MEASUREMENT.md`: fresh CUDA run `diffusion-0bd575b42c734811` completed `22` full generations and `8` probe generations, but all `8/8` planning rows had `source_task_delta_vs_trajectory = 0.000000`; the frozen floor would select only `plan_086` before labels, and no label pass can satisfy the declared source-divergence gate from this measurement. | This is a measurement-boundary result, not a repair-label transfer result; the label pass was intentionally not run because the predeclared source-divergence requirement already failed. | The boundary is overturned only by a committed protocol that forces nonzero source-vs-trajectory rows before labels and then replays the frozen value floor without threshold changes. | Build a stricter source-policy stress protocol that fixes distinct trajectory and repair-source choices before GPU labels, then rerun measurement before any label pass. |
| T42 | boundary | Random-source repair/probe sourcing is frozen as the next v11 source-divergence stress, but no random-source GPU result exists yet. | `DIFFUSION_COUNTERFACTUAL_SPAN_PROBE_VALUE_FLOOR_V11_RANDOM_SOURCE_FREEZE.md`: adds the runner policy `--repair-source-policy random`, freezes the same `lean_gpu_mixed_transfer_v11` task IDs and threshold `0.02891517987715706`, and records random-source measurement/label commands after fixed-source measurement failed to create source-vs-trajectory deltas. | The new policy deliberately uses the stable random arm as the repair/probe source while the trajectory selector remains `planning_state`; this should stress the trajectory channel but is still only a protocol until GPU measurement confirms nonzero source deltas. | The random-source stress fails if measurement still has no source-divergent rows, if labels are run before the random-source measurement gate is checked, or if the frozen floor misses positives or admits no-lift rows after labels. | Run the frozen random-source v11 measurement pass, verify source divergence before labels, then either run labels and replay the floor or record the second measurement-boundary failure. |
| T43 | boundary | The random-source v11 measurement pass satisfies source divergence, but labels are still pending. | `DIFFUSION_COUNTERFACTUAL_SPAN_PROBE_VALUE_FLOOR_V11_RANDOM_SOURCE_MEASUREMENT.md`: fresh CUDA run `diffusion-80ff9643f2b45f23` completed `22` full generations and `8` probe generations with `--repair-source-policy random`; `5/8` planning rows have negative `source_task_delta_vs_trajectory`, and the frozen floor would select `plan_081`, `plan_082`, and `plan_086` before labels. | This result proves the measurement gate is now meaningful, not that the floor transfers; repair labels and replay are still absent. | The random-source transfer claim fails if the frozen floor misses positive repairs, admits no-lift rows, loses utility after probe cost, or has zero positive repair labels and is therefore inconclusive. | Run the frozen random-source v11 label pass and replay the frozen value floor with false-positive, false-negative, utility, and measurement-cost accounting. |
| T44 | boundary | The measured probe-value floor fails the random-source v11 transfer replay. | `DIFFUSION_COUNTERFACTUAL_SPAN_PROBE_VALUE_FLOOR_V11_RANDOM_SOURCE_REPLAY.md`: fresh label run `diffusion-ff98d7df72f8d3ba` plus replay selects `3` rows, all false positives (`plan_081`, `plan_082`, `plan_086`), misses the only selected-repair positive (`plan_088`), covers `0.000000` positive lift, and scores `-0.060000` utility before probe cost and `-0.090000` after probe cost. | The replay uses the frozen threshold from v10 and the predeclared random-source measurement/label artifacts; it does not retune after seeing v11 labels. | The floor-failure claim fails if a source-aware controller using only pre-label measurement features explains why high probe value on negative-delta random sources is no-lift while `plan_088` is positive despite low probe value. | Build a source-aware signed-value or lift-direction model using `plan_081`, `plan_082`, `plan_086`, and `plan_088` as named counterexamples, then freeze before any new GPU slice. |
| T45 | boundary | The v11 falsifier demands a source-aware lift-direction model, not another probe-value floor. | `DIFFUSION_COUNTERFACTUAL_SPAN_PROBE_VALUE_FLOOR_V11_COUNTEREXAMPLES.md`: post-label counterexample analysis shows `plan_081`, `plan_082`, and `plan_086` are high-probe false positives on negative-delta random sources; `plan_088` is the selected-repair positive below the old floor; and `plan_084` is oracle-positive but selector-negative. The source-aligned moderate-gap hypotheses reduce the named selected-repair errors but are diagnostic-only. | This is a post-label analysis over the v11 random-source replay, so it can describe the failure geometry but cannot promote or freeze a new controller for live spend. | The source-aware reading fails if a predeclared model using only pre-label features cannot explain these named rows and transfer on a fresh slice without retuning. | Build and freeze a source-aware signed-value or lift-direction target surface before any new GPU slice, with separate accounting for repair-generation value and promotion-selector realization. |
| T46 | boundary | The source-aware lift-direction target is frozen for v12, but no fresh GPU result exists yet. | `DIFFUSION_SOURCE_AWARE_LIFT_DIRECTION_V12_FREEZE.md`: freezes `lean_gpu_mixed_transfer_v12` over new `plan_089`-`plan_096` rows plus `math_009`, `sym_007`, and `sci_002`; the manifest locks `source_task_delta_vs_trajectory >= 0`, `prompt_gap_count <= 4`, `prompt_coverage >= 0.7`, random-source measurement/label commands, selected-repair/oracle split, probe-cost accounting, controls, and zero overlap with v11 counterexample rows before v12 labels exist. | The target surface comes from post-label v11 counterexample analysis and is only a fresh-slice proof obligation; it is not a live spend trigger and does not treat probe value as a positive direction threshold. | The v12 freeze fails if task IDs overlap v11 counterexamples, if labels are generated before the freeze is committed, if the fresh replay misses selected-repair positives or admits no-lift rows, if oracle-positive selector misses remain for promotion, or if source/gap/coverage/probe controls match the combined surface. | Run the frozen v12 measurement and label passes, then replay selected-repair and oracle outcomes with source-only, gap-only, coverage-only, and probe-value controls plus probe-cost accounting. |
| T47 | boundary | The v12 source-aware measurement pass satisfies the source-divergence gate, but labels are still pending. | `DIFFUSION_SOURCE_AWARE_LIFT_DIRECTION_V12_MEASUREMENT.md`: fresh CUDA run `diffusion-36cf5bcb4eacd64e` completed `22` full generations and `8` probe generations; `plan_089`, `plan_090`, and `plan_095` have negative source-vs-trajectory deltas; the frozen surface selects only `plan_091` before labels while blocking high-probe disagreement rows `plan_089`, `plan_090`, `plan_092`, `plan_093`, `plan_094`, and `plan_095`. | This result proves the v12 measurement is a real source-divergence stress, not that the source-aware surface transfers; no repair labels or replay outcomes exist yet. | The measurement-boundary claim fails if the label command is changed after this point, if replay uses a different surface, or if labels are interpreted without selected-repair/oracle split and probe-cost accounting. | Run the frozen v12 label pass, then replay the source-aware surface with selected-repair false positives, selected-repair false negatives, oracle selector misses, source-only/gap-only/coverage-only/probe controls, and probe-cost accounting. |
| T48 | boundary | The frozen v12 source-aware lift-direction surface fails selected-repair replay. | `DIFFUSION_SOURCE_AWARE_LIFT_DIRECTION_V12_REPLAY.md`: fresh label run `diffusion-bb0e3d91e8840b1a` plus replay shows the frozen surface selects no-lift `plan_091`, misses selected-repair positive `plan_093`, leaves oracle-positive selector miss `plan_094`, and scores `-0.020000` utility before probe cost and `-0.050000` after probe cost. Source-only has no selected-repair false negatives but admits four selected-repair false positives; the probe-value floor control keeps no selected-repair false negatives but still has six selected-repair false positives and negative probe-cost utility. | The replay uses the frozen v12 measurement and label artifacts without retuning; this is a transfer failure of the static source/gap/coverage surface, not a failure of the no-leak protocol. | The v12 replay-failure claim fails if a predeclared denoise-phase realization channel explains `plan_093` without selecting `plan_091` and then transfers on a fresh slice. | Build and freeze a denoise-phase realization target that uses source alignment plus repairable denoise skeleton evidence before any new GPU slice. |
| T49 | boundary | The denoise-phase realization target is frozen for v13, but no fresh GPU result exists yet. | `DIFFUSION_DENOISE_PHASE_REALIZATION_V13_FREEZE.md`: freezes `lean_gpu_mixed_transfer_v13` over new `plan_097`-`plan_104` rows plus `math_009`, `sym_007`, and `sci_002`; the manifest locks source-aligned repairable denoise skeleton evidence, phase-window and denoise-coverage thresholds, random-source measurement/label commands, selected-vs-oracle replay, skeleton/source/static/probe controls, and zero overlap with v12 replay rows before v13 labels exist. | The v13 target is motivated by the v12 failure but not validated by it: v12 shows `plan_093` has source-aligned repairable skeleton evidence, while `plan_091` proves skeleton presence alone can be no-lift and `plan_094` separates oracle generation value from selected-repair realization. | The freeze fails if v13 task IDs overlap v12 replay rows, if labels are generated before the freeze is committed, if replay misses selected positives, admits no-lift skeleton rows, leaves oracle-positive selector misses for promotion, or if skeleton-only/source-only/static/probe controls match the combined target. | Run the frozen v13 measurement and label passes, then replay denoise-phase realization with selected-repair false positives, selected-repair false negatives, oracle selector misses, source-only, skeleton-only, static source/gap/coverage, probe-value, and probe-cost controls. |
| T50 | boundary | The v13 denoise-phase realization measurement pass is meaningful, but labels are still pending. | `DIFFUSION_DENOISE_PHASE_REALIZATION_V13_MEASUREMENT.md`: fresh CUDA run `diffusion-8196af06c90923b1` completed `22` full generations and `8` probe generations; `plan_097` has positive random-source delta, `plan_098` has negative random-source delta, all eight planning rows have repairable denoise skeletons, and the frozen realization surface selects `plan_099` plus `plan_102` before labels while blocking high-probe rows `plan_098` and `plan_101`. | This result proves the v13 measurement is a real pre-label stress of the denoise realization target. It does not prove transfer: no repair labels or replay outcomes exist yet, and skeleton-only being universal means labels must decide whether phase/coverage specificity adds value. | The measurement-boundary claim fails if the label command is changed after this point, if replay uses a different surface, or if labels are interpreted without selected-repair/oracle split, skeleton-only controls, source controls, and probe-cost accounting. | Run the frozen v13 label pass, then replay denoise-phase realization with selected-repair false positives, selected-repair false negatives, oracle selector misses, skeleton-only/source-only/static/probe controls, and probe-cost accounting. |
| T51 | boundary | The frozen v13 denoise-phase realization surface fails selected-repair promotion, even though the broader label trigger finds lift. | `DIFFUSION_DENOISE_PHASE_REALIZATION_V13_REPLAY.md`: fresh label run `diffusion-567d84d21ee7c484` plus replay shows selected-repair positives `plan_099` and `plan_104`; the frozen surface selects `plan_099` and no-lift `plan_102`, misses `plan_104`, and scores `0.068929` utility after probe cost. Skeleton/source/phase controls preserve recall but admit five or six no-lift rows, while the label-pass denoise trigger preserves recall with false positives `plan_098`, `plan_100`, and `plan_101` and `0.190714` utility after probe cost. | The replay uses the frozen v13 measurement and label artifacts without retuning. It supports denoise-phase evidence as a valuable channel but rejects the specific source-aligned phase/coverage surface as a promotable spend gate. | The v13 replay-failure claim fails if a predeclared realization-value head can preserve `plan_099` and `plan_104`, reject `plan_102`, and improve utility against skeleton/source/phase/probe controls on a fresh slice without retuning. | Build a realization-value counterexample map around `plan_099`, `plan_102`, and `plan_104`, then freeze a v14 target before any new GPU labels. |
| T52 | boundary | The v13 counterexample map yields a zero-error realization-value diagnostic fit, but it is not transfer evidence. | `DIFFUSION_DENOISE_PHASE_REALIZATION_V13_COUNTEREXAMPLES.md`: the fitted diagnostic rule `label_trigger_source_nonnegative_gap_4_to_7_probe_le_0p032` selects `plan_099` and `plan_104` with zero selected-repair errors and `0.250714` utility after probe cost on v13, while the broad denoise trigger admits no-lift `plan_098`, `plan_100`, and `plan_101`, and the frozen v13 surface admits no-lift `plan_102` while missing `plan_104`. | This is post-label v13 analysis, useful for mechanism design but not promotable. It fits named counterexamples and must be frozen before a fresh slice to mean anything. | The counterexample-map claim fails if the generated analysis no longer identifies the named v13 false positive, false negative, broad-trigger false positives, selected positives, and oracle-only selector misses. | Build a frozen realization-value target on a fresh v14 slice with no task overlap and replay it against selected-repair and oracle labels. |
| T53 | boundary | The realization-value target is frozen for v14, but no fresh GPU result exists yet. | `DIFFUSION_REALIZATION_VALUE_V14_FREEZE.md`: freezes `lean_gpu_mixed_transfer_v14` over new `plan_105`-`plan_112` rows plus `math_009`, `sym_007`, and `sci_002`; the manifest locks a denoise-trigger plus source-nonnegative plus prompt-gap `4`-to-`7` plus probe-value `<=0.032` target, random-source measurement/label commands, selected-vs-oracle replay controls, probe-cost accounting, and zero overlap with v13 counterexample rows before v14 labels exist. | The target comes from post-label v13 counterexamples and is only a fresh-slice proof obligation. It is not a live spend trigger and requires a measurement/label join plus replay controls before interpretation. | The freeze fails if v14 task IDs overlap v13 counterexamples, if labels are generated before the freeze is committed, if replay misses positives or admits no-lift rows, if broad denoise triggering is simpler and equally good after cost, or if oracle-only wins are counted as deployed value. | Run the frozen v14 measurement and label passes, then replay realization value against label-trigger, skeleton, source, static, probe, and oracle controls. |
| T54 | boundary | The v14 realization-value measurement pass fails before labels because the frozen target is empty. | `DIFFUSION_REALIZATION_VALUE_V14_MEASUREMENT.md`: fresh CUDA run `diffusion-22840dc9c690b7c0` completed `22` full generations and `8` probe generations; source-divergent rows appear on `plan_105`, `plan_107`, `plan_108`, and `plan_112`, but the frozen target selects zero rows before labels. `plan_109` and `plan_112` satisfy the source and prompt-gap band but miss only the frozen probe cap by small margins (`0.032445` and `0.032383` versus `0.032000`). | This is a measurement-boundary failure, not a repair-label transfer result. Running labels from an empty target would not test the predeclared recall or specificity gates. | The measurement-boundary claim fails if the analyzer selects any frozen target row from the committed v14 measurement or if labels are interpreted as v14 transfer evidence despite the empty pre-label target. | Build a no-label addendum or fresh v14b slice that tests whether the probe cap was overfit before any repair labels are generated. |
| T55 | boundary | The v14b realization-value addendum is frozen before labels to test whether the original probe cap was over-specific. | `DIFFUSION_REALIZATION_VALUE_V14B_ADDENDUM.md`: generated from the committed v14 freeze and measurement boundary while no v14 or v14b label scores exist; the addendum keeps source-nonnegative plus prompt-gap `4`-to-`7`, relaxes only `measured_probe_value_prediction` from `<=0.032` to `<=0.033`, selects `plan_109` and `plan_112` before labels, and requires denoise-trigger joining plus selected-repair/oracle replay controls before interpretation. | This is a measurement-only addendum, not a transfer result or live spend trigger. Its value is to test cap overfit on the already measured but still unlabeled v14 slice without looking at repair-label outcomes. | The addendum claim fails if v14 or v14b labels existed before generation, if the selected set is empty, if the addendum uses label information, if replay admits no-lift rows or misses selected-repair positives, or if broad denoise triggering is simpler after probe-cost accounting. | Run the frozen v14b label pass, then replay v14 and v14b against label-trigger, skeleton, source, static, probe, oracle, and probe-cost controls. |
| T56 | boundary | The v14b realization-value replay rescues the empty v14 target on this fresh slice, but remains replay evidence rather than a live trigger. | `DIFFUSION_REALIZATION_VALUE_V14B_REPLAY.md`: fresh label run `diffusion-c9a25cfe3d8aa862` completed `27` full generations; original v14 selects no rows and misses selected-repair positives `plan_109` and `plan_112`, while v14b selects exactly `plan_109` and `plan_112` after denoise-trigger joining with zero selected-repair false positives, zero false negatives, and `0.131929` utility after probe cost. Broad denoise triggering selects `plan_106`, `plan_108`, and `plan_110` as no-lift false positives, skeleton-only selects six no-lift rows, source-only selects three no-lift rows, and probe-only controls fail recall or specificity. | The replay supports a realization-value filter on this v14 slice, not unconditional denoise triggering and not a deployed spend gate. The static source/gap/coverage control also selects the positives here, so the next proof obligation must separate whether the probe cap adds transfer value beyond static banding. | The replay claim fails if the committed replay no longer joins v14b to the denoise label trigger, if selected-repair labels change, if broad controls stop showing no-lift rows, if probe cost is omitted for probe-based policies, or if the result is promoted without a live runner implementation and another fresh slice. | Build the next fresh slice or runner-level implementation test that separates static source/gap/coverage banding from probe-conditioned realization value. |
| T57 | boundary | The v15 static-vs-probe disagreement slice is frozen before measurement or labels. | `DIFFUSION_REALIZATION_VALUE_V15_FREEZE.md`: freezes `lean_gpu_mixed_transfer_v15` over new `plan_113`-`plan_120` rows plus `math_009`, `sym_007`, and `sci_002`; the manifest records that v14b probe-conditioned and static source/gap/coverage surfaces both selected `plan_109` and `plan_112` with zero selected-repair errors, then predeclares separate `static_source_gap_coverage_v15` and `probe_conditioned_realization_value_v15` surfaces, measurement and label commands, minimum disagreement and positive-label gates, probe-cost accounting, and no live-trigger status before v15 measurement or labels exist. | The frozen slice is designed to falsify the extra information value of the measured probe, not to prove promotion. A conclusive result requires static-vs-probe disagreement rows plus at least one selected-repair positive; otherwise the slice is only a measurement boundary. | The v15 freeze claim fails if v15 measurement or label scores existed before the freeze, if task IDs overlap v14b replay rows, if the replay omits static-vs-probe disagreement accounting, if probe cost is omitted, or if static controls match the probe surface and the result is still treated as support. | Run the frozen v15 measurement pass, analyze static-vs-probe disagreement before labels, and run labels only if the measurement can test the T56 proof obligation. |
| T58 | boundary | The v15 measurement pass creates a pre-label static-vs-probe disagreement and authorizes labels, but proves no transfer result yet. | `DIFFUSION_REALIZATION_VALUE_V15_MEASUREMENT.md`: fresh CUDA run `diffusion-9310814fffeb6d04` completed `22` full generations and `8` probe generations; the static source/gap/coverage surface selects `plan_120`, the probe-conditioned surface selects zero rows, and `plan_120` is the single static-only disagreement row because its measured probe value `0.038662` exceeds the frozen `0.033000` cap. | This is a measurement-boundary artifact only. It authorizes the frozen v15 label pass because static and probe surfaces now disagree before labels, but transfer interpretation still requires denoise-trigger label joining, selected-repair false-positive/false-negative accounting, oracle misses, controls, and probe-cost accounting. | The measurement-boundary claim fails if committed v15 measurement rows no longer contain a static-only disagreement, if labels are interpreted before replay, if probe costs are omitted, or if the label command is changed after measurement. | Run the frozen v15 label pass, then replay static and probe-conditioned surfaces against selected-repair labels, oracle-positive misses, broad-trigger/skeleton/source/gap/coverage/probe controls, and utility after probe cost. |
| T59 | boundary | The v15 replay falsifies the added probe cap on this slice; static banding beats the probe-conditioned surface, but no live controller is promoted. | `DIFFUSION_REALIZATION_VALUE_V15_REPLAY.md`: fresh label run `diffusion-1b2da43bad7a69b0` completed `29` full generations; selected-repair positive `plan_120` is static-selected and probe-rejected, so `static_source_gap_coverage_v15` has zero selected-repair false positives and false negatives with `0.127857` utility, while `probe_conditioned_realization_value_v15` selects no rows, false-negatives `plan_120`, and scores `-0.030000` after probe cost. Broad denoise triggering selects six no-lift rows, skeleton-only selects seven no-lift rows, and `plan_118` remains an oracle-positive selector miss. | The result rejects the measured probe cap as a transfer-value condition on v15, not diffusion repair itself. Static banding is still only replay evidence, because oracle-positive `plan_118` shows the selector misses generation value and no runner-level live trigger has been implemented. | The replay claim fails if the committed replay no longer joins static/probe surfaces to the label trigger, if `plan_120` is not a selected-repair positive, if the probe surface no longer false-negatives it, if broad controls stop admitting no-lift rows, or if probe cost is omitted. | Build the next proof obligation around why `plan_118` is oracle-positive but selector-negative, and keep probe measurements diagnostic until a fresh predeclared surface beats static controls after cost. |
| T60 | boundary | The v15 candidate-promotion target sheet exposes a low-margin promotion miss and freezes v16 to test candidate-realization features before labels. | `DIFFUSION_CANDIDATE_PROMOTION_TARGETS_V15.md`: generated repair candidates have positives `plan_118` and `plan_120`; incumbent `candidate_aware_promotion_v1` selects only `plan_120`, making one promotion error on low-margin `plan_118` (`+0.012500` candidate lift). `DIFFUSION_PROMOTION_MARGIN_V16_FREEZE.md` freezes `lean_gpu_mixed_transfer_v16` over new `plan_121`-`plan_128` rows plus guards, with `low_margin_candidate_realization_v16` requiring nonnegative source delta, prompt gap `4`-to-`8`, prompt coverage `0.4`-to-`1.0`, positive planning-quality delta over source, span preservation at least `0.90`, and span score at least `2.10`. | The v16 surface is a replay proof obligation, not a live trigger. It is motivated by v15 labels and must be tested on fresh rows before any implementation or promotion language. The source-relative trap `plan_113` means candidate lift must be measured against the selected trajectory, not merely against the repair source. | The freeze claim fails if v16 labels existed before the freeze, if v16 task IDs overlap v15 promotion rows, if the replay counts source-only lift as promotion, if the low-margin surface admits no-lift rows or misses generated positives, or if the incumbent selector matches it. | Run the frozen v16 label pass, build candidate-promotion targets from raw repair candidates, and replay the low-margin surface against incumbent, source-lift-only, span-only, planning-delta-only, broad-trigger, and margin-free controls. |
| T61 | boundary | The v16 promotion-margin slice is inconclusive for margin relaxation because no generated repair candidate beats the selected trajectory. | `DIFFUSION_CANDIDATE_PROMOTION_TARGETS_V16.md`: fresh label run artifacts `promotion_margin_v16_label_*` produce `7` generated repair-candidate target rows, `0` generated positives, and `0` candidate-aware promotion errors; all candidate lifts versus trajectory are non-positive. `promotion_margin_v16_label_scores.json` still shows `plan_128` as oracle-positive by `+0.020000`, but that value is outside the generated-candidate promotion target because the generated repair candidate is below trajectory. | This result does not validate the low-margin v16 surface; it fails the predeclared minimum generated-positive count. The useful lesson is that the bottleneck moved upstream from promotion margin to generation/source search on this slice. | The inconclusive-boundary claim fails if the committed v16 target sheet contains any generated-positive repair candidate, if oracle-only `plan_128` is counted as deployed selected-repair value, or if v16 is described as validating the margin-relaxed surface. | Build the next proof obligation around recovering oracle-positive but generated-candidate-negative rows like `plan_128`, with source-search or candidate-diversity controls before another margin selector test. |
| T62 | boundary | The v17 source-preservation slice is frozen to test source-positive repair-degradation before another promotion-margin change. | `DIFFUSION_SOURCE_PRESERVATION_V17_FREEZE.md`: new `lean_gpu_mixed_transfer_v17` tasks `plan_129`-`plan_136` are frozen before labels, with `source_positive_repair_degradation_v17` requiring a positive source delta versus trajectory, non-positive generated-candidate lift versus trajectory, and negative candidate lift versus source. The fit boundary is the committed v16 `plan_128` row: source delta `+0.020000`, generated-candidate lift versus trajectory `-0.020000`, generated-candidate lift versus source `-0.040000`, and oracle delta `+0.020000`. | This is a replay proof obligation, not a live trigger. It treats direct source selection, generated repair promotion, and candidate diversity as separate controls so source-search value is not confused with repaired-candidate value. | The freeze claim fails if v17 labels existed before the freeze, if v17 task IDs overlap v16 promotion rows, if `plan_128` is no longer source-positive and repair-degraded in the pinned v16 artifacts, if source-only wins are counted as deployed repair value, or if zero source-positive rows are described as success rather than inconclusive. | Run the frozen v17 label pass, build source-positive repair-degradation targets, and replay direct-source, generated-repair, source-delta-only, candidate-degradation-only, span-only, and broad-denoise controls with utility after cost. |
| T63 | boundary | The v17 source-preservation gate is inconclusive, but the fresh slice restores generated repair positives on three planning rows. | `DIFFUSION_SOURCE_PRESERVATION_TARGETS_V17.md`: fresh label run `diffusion-7c7da63c19349927` completed `28` full generations and produced `8` planning target rows. The frozen source-preservation surface has `0` source-positive repair-degradation rows and `0` source-positive tasks, so it is inconclusive. Generated repair positives appear on `plan_129`, `plan_130`, and `plan_131` with candidate lifts versus trajectory of `+0.037500`, `+0.050000`, and `+0.050000`; the selected repair arm improves repair-covered task score by `+0.022187` versus fixed and `+0.045125` versus random in the run report. | This result does not validate direct source preservation because the fresh slice never recreates the `plan_128` source-positive/degraded-repair geometry. It does show that candidate generation can recover value on fresh rows, so the next proof obligation should split generated repair positives from direct-source preservation rather than collapsing them. | The result claim fails if any v17 target row is source-positive repair-degraded, if generated repair positives are absent from the target sheet, if source-preservation is described as validated, or if repair-covered lift is reported without the generation-budget cost. | Build the next replay around generated-repair positives `plan_129`-`plan_131`: compare candidate-aware selection, margin-free selection, source-delta controls, span controls, and broad-denoise controls with utility after repair-generation cost. |
| T64 | boundary | The v18 generated-repair slice is frozen to test whether the v17 generated repair positives repeat on fresh rows. | `DIFFUSION_GENERATED_REPAIR_V18_FREEZE.md`: new `lean_gpu_mixed_transfer_v18` tasks `plan_137`-`plan_144` are frozen before labels, with `generated_repair_value_v18` requiring a denoise-triggered generated repair candidate, nonnegative source delta versus trajectory, positive candidate lift versus trajectory, positive candidate lift versus source, prompt gap `2`-to-`6`, and prompt coverage `0.60`-to-`0.90`. The fit boundary is the committed v17 target sheet: generated repair positives `plan_129`, `plan_130`, and `plan_131`, minimum positive candidate lift `+0.037500`, and source-positive repair-degradation count `0`. | This is a replay proof obligation, not a live generated-repair trigger. It follows the v17 result by testing generated repair value directly while keeping source-preservation and source-search claims separate. | The freeze claim fails if v18 labels existed before the freeze, if v18 task IDs overlap v17 target rows, if the pinned v17 target sheet no longer has generated positives `plan_129`-`plan_131`, if source-positive preservation is claimed from this freeze, or if repair generation cost is omitted from the conclusive gates. | Run the frozen v18 label pass, build generated-repair promotion targets, and replay candidate-aware margin, margin-free, source-delta-only, span-only, gap-only, and broad-denoise controls with task lift per extra generation. |
| T65 | boundary | The v18 generated-repair value repeats on fresh rows and the incumbent candidate-aware selector captures the positives on this slice. | `DIFFUSION_CANDIDATE_PROMOTION_TARGETS_V18.md`: fresh label run `diffusion-da860927095a60e2` completed `26` full generations and produced `4` generated repair-candidate target rows. Positive generated repairs are `plan_137` and `plan_139`, with candidate lifts versus trajectory of `+0.160357` and `+0.122857`; negatives are `plan_141` and `plan_144`. `candidate_aware_promotion_v1` selects `plan_137` and `plan_139` with `0` promotion errors. The run report shows selected latent repair improves repair-covered task score by `+0.035223` versus fixed and `+0.058652` versus random, with task delta per extra generation `+0.071`. | This validates the replay boundary for generated-repair value on v18, not a live controller. The next implementation boundary is to turn the replay evidence into a runner-level generated-repair value hook or continue fresh transfer before promotion language. | The result claim fails if the v18 target sheet no longer has exactly two positives, if candidate-aware selector errors appear, if the run ID or lift values are not in the committed artifacts, if source-preservation is claimed from this result, or if repair cost is omitted. | Build either a runner-level generated-repair value hook with tests and fresh validation, or freeze another transfer slice to test whether the same generated-repair value holds outside the current prompt family. |
| T66 | boundary | `generated_repair_value_v1` implements the v18 generated-repair value boundary as a label-free runner hook and reproduces the v18 target split on no-generation replay. | `DIFFUSION_GENERATED_REPAIR_VALUE_HOOK_V1.md`, `eval_results/diffusion_language/generated_repair_v18_value_hook_scores.json`, and `eval_results/diffusion_language/generated_repair_v18_value_hook_report.md`: no-generation replay `diffusion-d4a90959bf5734b2` reuses committed v18 raw rows, selects generated-repair positives `plan_137` and `plan_139`, rejects no-lift generated repairs `plan_141` and `plan_144`, and preserves the v18 repair-covered lifts of `+0.035223` versus fixed and `+0.058652` versus random with task delta per extra generation `+0.071`. Focused tests assert that `generated_repair_value_v1` scores `plan_137` above the `0.02` promotion margin and scores no-lift `plan_141` as zero. | This is an implementation and replay check only. It is label-free at selection time, but its evidence still comes from v18 replay rows and does not promote the hook as a final live controller until a fresh slice runs with this selector. | The hook claim fails if the selector no longer selects v18 positives and rejects no-lift rows, if the committed rescore artifacts lose run ID `diffusion-d4a90959bf5734b2` or the reported lifts, if fresh live validation loses repair utility after generation cost, or if the hook is described as source-preservation evidence. | Build a fresh hook-validation slice with `--repair-selector generated_repair_value_v1`, then compare selected rows, no-lift rejections, and task lift per extra generation against candidate-aware and broad-denoise controls. |
| T67 | boundary | The v19 generated-repair hook-validation slice is frozen before labels to test the implemented selector on fresh rows. | `DIFFUSION_GENERATED_REPAIR_VALUE_HOOK_V19_FREEZE.md`: freezes `lean_gpu_mixed_transfer_v19` over new `plan_145`-`plan_152` rows plus `math_009`, `sym_007`, and `sci_002`, with zero overlap against v18 hook fit rows. The fit boundary is the committed v18 hook replay `diffusion-d4a90959bf5734b2`, which selected `plan_137` and `plan_139`, rejected no-lift rows `plan_141` and `plan_144`, and reported repair-covered lift `+0.035223` versus fixed, `+0.058652` versus random, and `+0.071` task delta per extra generation. The frozen command runs `--repair-selector generated_repair_value_v1` with the same repair source, spend trigger, repair pack, margin, trajectory selector, CUDA device, and bfloat16 dtype. | This is a pre-label validation boundary, not a result. It tests whether the implemented label-free hook transfers on fresh rows and keeps broad denoise, candidate-aware, source-bucket, selector-regret, and repair-cost accounting as required controls. | The freeze claim fails if v19 labels existed before the freeze, if v19 planning IDs overlap v18 fit rows, if the command changes after labels exist, if the hook selects nonpositive task-lift rows, if broad denoise matches after cost, or if zero generated positives are described as success. | Run the frozen v19 label command, then report selected-repair positives, no-lift selected rows, broad-denoise and candidate-aware controls, source buckets, selector regret, oracle headroom, and task lift per extra generation. |
| T68 | boundary | The implemented `generated_repair_value_v1` hook transfers on the fresh v19 slice as a local cost-positive selector, with one bounded low-margin miss. | `DIFFUSION_GENERATED_REPAIR_VALUE_HOOK_V19_RESULT.md`, `DIFFUSION_GENERATED_REPAIR_VALUE_HOOK_V19_TARGETS.md`, `eval_results/diffusion_language/generated_repair_value_hook_v19_label_scores.json`, and `eval_results/diffusion_language/generated_repair_value_hook_v19_label_report.md`: fresh CUDA run `diffusion-e4db8307fba01a16` completed `27` full generations with `--repair-selector generated_repair_value_v1`. The target sheet has `5` generated repair rows and `4` positives: `plan_145`, `plan_149`, `plan_151`, and `plan_152`; `plan_150` is the no-lift negative. The hook selected `plan_145`, `plan_149`, and `plan_152`, rejected `plan_150`, and missed only low-margin `plan_151` with candidate lift `+0.007000` and zero planning-quality delta versus source. Repair-covered selected latent repair improves task score by `+0.044286` versus fixed and `+0.075777` versus random, with repair task delta per extra generation `+0.071`, oracle headroom `0.001`, and selector regret `0.001 over 1/8 improvable`. | This supports the hook locally on v19 but does not prove source preservation or broad controller promotion. The miss is intentionally not retuned away because it is below the frozen `0.02` margin and lacks the hook's source-relative planning-quality signal. | The result claim fails if the committed v19 target sheet no longer has four positives and one negative, if the hook-selected rows include no-lift `plan_150`, if the aggregate lift or run ID disappears from the scores/report, if selector regret grows under replay, if broad denoise matches after cost, or if global promotion language is attached to this local slice. | Run another transfer slice or a broad-denoise control replay before promoting the hook beyond local generated-repair selection, and preserve `plan_151` as the next low-margin counterexample. |
| T69 | boundary | The v19 permissive-control replay blocks broad promotion while preserving the hook's precision reading. | `DIFFUSION_GENERATED_REPAIR_VALUE_HOOK_V19_CONTROL_REPLAY.md`, `eval_results/diffusion_language/generated_repair_value_hook_v19_broad_denoise_control_scores.json`, and `eval_results/diffusion_language/generated_repair_value_hook_v19_broad_denoise_control_report.md`: no-generation control replay `diffusion-6841660796b6d7c6` reuses committed v19 raw candidates with `--repair-selector planning_quality` and `--repair-promotion-margin 0.0`. It matches the hook's repair-covered utility exactly: `+0.044286` versus fixed, `+0.075777` versus random, `+0.071` task delta per extra generation, oracle headroom `0.001`, and selector regret `0.001 over 1/8`. But the permissive control selects `plan_150` and `plan_151` in addition to shared substantial positives `plan_145`, `plan_149`, and `plan_152`; `plan_150` is no-lift and `plan_151` is only `+0.007000` with zero planning-quality delta versus source. | This means the hook is not globally dominant on aggregate v19 utility; its current value is cleaner allocation precision over already-generated repair candidates. The replay is a control over promotion precision, not a fresh availability or spend-trigger generation test. | The control claim fails if the committed control replay no longer matches the hook's aggregate utility, if it stops selecting `plan_150` or `plan_151`, if `plan_150` becomes positive in the target sheet, if the hook is described as aggregate-dominant over this control, or if this no-generation replay is treated as a fresh GPU validation. | Build a new proof obligation that prices unnecessary selected repairs explicitly, or freeze a low-margin selector slice that tries to recover `plan_151` without admitting no-lift `plan_150`. |
| T70 | boundary | Explicit selected-output pricing makes `generated_repair_value_v1` dominate the permissive v19 control for any positive selected-repair cost. | `DIFFUSION_GENERATED_REPAIR_VALUE_HOOK_V19_SELECTED_COST_AUDIT.md` and `eval_results/diffusion_language/generated_repair_value_hook_v19_selected_cost_audit.json`: the selected-cost audit compares the v19 hook run `diffusion-e4db8307fba01a16` against permissive control replay `diffusion-6841660796b6d7c6` using the same target sheet. Both policies have identical repair task delta versus evolved `+0.044286` and delta per extra generation `+0.070857`, but the hook selects `3` repair-pool outputs and `0` waste rows while the control selects `5` repair-pool outputs with `2` waste rows, `plan_150` and `plan_151`. Under selected-output costs `0.001`, `0.005`, `0.010`, and `0.020`, hook net exceeds control net by `0.000250`, `0.001250`, `0.002500`, and `0.005000` respectively. | This is a cost-accounting result over already-generated v19 candidates. It strengthens the hook's precision claim but still does not prove broad availability, source preservation, or global controller promotion. | The selected-cost claim fails if the audit no longer shows aggregate utility parity, if the control no longer has two extra waste selections, if the hook no longer has fewer selected repair-pool outputs, if any positive selected-output cost does not favor the hook, or if this selected-output audit is substituted for a fresh transfer slice. | Build either a low-margin selector slice that recovers `plan_151` without admitting `plan_150`, or a broader availability slice that tests whether selected-output cost still favors the hook when repair candidates differ. |
| T71 | boundary | The v20 low-margin repair slice is frozen before labels to test whether `plan_151`-style tiny source-tie positives recur without admitting `plan_150`-style no-lift rows. | `DIFFUSION_LOW_MARGIN_REPAIR_V20_FREEZE.md`: freezes `lean_gpu_mixed_transfer_v20` over new `plan_153`-`plan_160` rows plus `math_009`, `sym_007`, and `sci_002`, with zero overlap against v19 target rows. The fit boundary is `eval_results/diffusion_language/generated_repair_value_hook_v19_targets.json` plus `eval_results/diffusion_language/generated_repair_value_hook_v19_selected_cost_audit.json`: `plan_151` is the tiny positive with candidate lift `+0.007000`, zero planning-quality delta versus source, and max span score `0.000000`; `plan_150` is the no-lift counterexample with candidate lift `0.000000` and max span score `3.810750`; both are control-only waste rows in the selected-cost audit. The frozen command uses permissive `--repair-selector planning_quality` and `--repair-promotion-margin 0.0` to expose low-margin candidates for replay rather than promoting a fallback. | This is a pre-label proof obligation, not a live fallback. It tests whether a low-margin source-tie layer can add value after selected-output cost while keeping no-lift false positives at zero. | The freeze claim fails if v20 labels existed before the freeze, if v20 IDs overlap v19 target rows, if the command changes after labels exist, if no tiny positives appear and the result is called success, if any no-lift row matches the low-margin surface, or if the fallback is merged into the main hook before fresh validation. | Run the frozen v20 label command, build the candidate-promotion target sheet, then replay the main hook, permissive control, and low-margin surface with selected-output cost accounting. |
| T72 | boundary | The v20 low-margin fallback gate is not validated because the fresh slice has zero generated repair positives. | `DIFFUSION_LOW_MARGIN_REPAIR_V20_RESULT.md`, `DIFFUSION_LOW_MARGIN_REPAIR_V20_TARGETS.md`, `eval_results/diffusion_language/low_margin_repair_v20_label_scores.json`, and `eval_results/diffusion_language/low_margin_repair_v20_label_report.md`: fresh CUDA run `diffusion-19d1c9173d08bc15` completed `26` full generations under the frozen permissive label command. The generated repair target sheet has `4` rows and `0` positives; negative tasks are `plan_154`, `plan_155`, `plan_158`, and `plan_160`. Candidate lifts versus trajectory are `-0.043000`, `-0.063000`, `-0.127286`, and `-0.130000`. Repair task delta versus evolved is `0.000000`, task delta per extra generation is `0.000000`, and oracle headroom versus repair is `0.000000`. | This kills promotion of the low-margin fallback on v20 rather than weakening the main hook. High-gap or span-looking generated repairs remain insufficient when task-relative candidate lift is negative. | The result claim fails if the v20 target sheet gains any positive row, if zero positives are described as validating the fallback, if negative candidate lifts are ignored, if the main hook is changed based on this failed fallback, or if repair utility is reported without the zero-delta cost accounting. | Build the next proof obligation around broader repair availability or source/candidate diversity, not low-margin margin relaxation, and keep `generated_repair_value_v1` unchanged until fresh positive candidates appear. |
| T73 | boundary | The v21 candidate-diversity slice is frozen before labels to test whether broader repair availability creates positives after the v20 zero-positive boundary. | `DIFFUSION_CANDIDATE_DIVERSITY_V21_FREEZE.md` and `eval_results/diffusion_language/candidate_diversity_v21_freeze.json`: freezes `lean_gpu_mixed_transfer_v21` over new `plan_161`-`plan_168` rows plus `math_009`, `sym_007`, and `sci_002`, with zero overlap against v20 target rows. The fit boundary pins v20 to `4` target rows, `0` positives, negative tasks `plan_154`, `plan_155`, `plan_158`, and `plan_160`, and repair delta versus evolved `0.000000`. The frozen command keeps `generated_repair_value_v1` and the final-preserve repair pack but adds `--include-history-repairs`, `--history-repair-fractions 0.25`, and `--limit-repair-candidates 2`, producing candidate order `history_prefix_25_repair` then `constraint_gap_span_phase_final_preserve_seeded_gated_repair`. | This is an upstream availability/source-diversity proof obligation, not a selector retune. The main hook remains unchanged until a fresh two-candidate run produces selected positives without selected no-lift rows and survives selected-output cost. | The freeze claim fails if v21 labels existed before the freeze, if v21 IDs overlap v20 target rows, if the candidate order changes after labels exist, if zero positives are described as success, if positives appear only in unselected rows and are called hook success, if selected no-lift rows appear, or if selected-output cost erases lift. | Run the frozen v21 CUDA command, build candidate-promotion targets from raw candidates, summarize positives by candidate name, replay a one-candidate control, and run selected-output cost accounting before changing the hook or broadening the live pool. |

## Current Strongest Theory Statement

The strongest statement the repo can currently defend is:

Diffusion-native latent reasoning improves the current lean GPU mixed benchmark
when a verifier-visible failure appears in an editable denoise trajectory, a
cost-aware selector estimates that repair is worth the extra generation, and the
repair preserves stable task constraints.

This is stronger than "we sampled more" and narrower than "general reasoning is
solved."

## Open Proof Obligations

1. Equal-cost AR rewrite baseline: compare diffusion repair to an
   autoregressive rewrite system with the same verifier spans, judge access, and
   generation budget.
2. Transfer slice: test whether `first_repairable_step`, `prompt_gap_count`,
   `source_quality`, `source_task - selected_task`, retention loss, and realization loss
   predict marginal repair value outside the current eight planning tasks.
3. Information-channel accounting: every promoted theory claim should state
   what came from prompt, model, denoise state, verifier, judge, and anchor.
4. Retention reporting: every new repair operator should report whether it
   preserves prompt terms, digits, roles, target spans, and compact controls.
5. Learned selector: replace fixed thresholds with a small predictor trained on
   generated marginal-value targets, then evaluate on held-out tasks.
6. Decomposed proof object: train availability, promotion-value, source-trust,
   retention, and cost heads, then test whether the combined error function
   beats a single repairability scorer on fresh GPU slices.

## How To Update This Ledger

When a new theory claim is added:

1. Add it as a row with a conservative status.
2. Link to score/report/audit artifacts, not only prose.
3. State what would falsify it.
4. Add the next proof obligation as an executable benchmark or audit.
5. Promote the status only after generated artifacts and validators support it.
6. Run `python experiments\validate_diffusion_theory_claim_ledger.py` before treating the ledger as public-current.
