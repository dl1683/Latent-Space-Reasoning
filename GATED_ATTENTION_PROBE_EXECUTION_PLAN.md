# Gated Attention Probe Execution Plan

This file is generated by `experiments/build_gated_attention_probe_execution_plan.py`.

## Status

- Freeze: `eval_results\gated_attention\gated_attention_null_probe_freeze.json`
- Probe ID: `gated_attention_null_probe_v1`
- Primary model: `Qwen/Qwen3-Next-80B-A3B-Instruct`
- Ready for primary GPU run: `False`

Blocking reasons:

- primary Qwen3-Next weights are not cached locally; select and record a current quantized artifact before starting the expensive run
- local Transformers does not support model_type=qwen3_next; install a supporting release/source build before using the Transformers soft-prefix runner

## Model Cache

| Role | Model | Cache dir | Weights | Snapshots | Cache path |
|---|---|---:|---:|---:|---|
| `baseline` | `Qwen/Qwen3-4B` | `True` | `True` | `1` | `C:\Users\devan\.cache\huggingface\hub\models--Qwen--Qwen3-4B` |
| `mechanics` | `Qwen/Qwen3-0.6B` | `True` | `True` | `1` | `C:\Users\devan\.cache\huggingface\hub\models--Qwen--Qwen3-0.6B` |
| `primary_gated` | `Qwen/Qwen3-Next-80B-A3B-Instruct` | `True` | `False` | `1` | `C:\Users\devan\.cache\huggingface\hub\models--Qwen--Qwen3-Next-80B-A3B-Instruct` |

## Runtime Compatibility

- Transformers version: `4.56.2`
- Supports `qwen3_next`: `False`


## Ordered Runs

### 1. `mechanics_position_shift_smoke`

- Claim status: `mechanics_only`
- Expected artifact: `eval_results/gated_attention/qwen3_06b_position_shift_mechanics_smoke.json`

```powershell
python -u experiments/run_latent_sensitivity.py --model Qwen/Qwen3-0.6B --task-type nested --difficulty sweet_spot --n-tasks 2 --control-mode position_shift --num-soft-tokens 2 --quantization 4bit --max-new-tokens 64 --output eval_results/gated_attention/qwen3_06b_position_shift_mechanics_smoke.json
```

### 2. `primary_position_shift_control`

- Claim status: `required_primary_control`
- Expected artifact: `eval_results\gated_attention\qwen3_next_position_shift_control_result.json`

```powershell
python -u experiments/run_latent_sensitivity.py --model Qwen/Qwen3-Next-80B-A3B-Instruct --task-type nested --difficulty sweet_spot --n-tasks 25 --control-mode position_shift --num-soft-tokens 2 --quantization 4bit --output eval_results/gated_attention/qwen3_next_position_shift_control_result.json
```

### 3. `primary_zero_prefix_control`

- Claim status: `required_primary_control`
- Expected artifact: `eval_results\gated_attention\qwen3_next_zero_prefix_result.json`

```powershell
python -u experiments/run_latent_sensitivity.py --model Qwen/Qwen3-Next-80B-A3B-Instruct --task-type nested --difficulty sweet_spot --n-tasks 25 --n-latents 1 --control-mode zero_embedding --num-soft-tokens 2 --quantization 4bit --output eval_results/gated_attention/qwen3_next_zero_prefix_result.json
```

### 4. `primary_random_prefix_n10`

- Claim status: `primary_test`
- Expected artifact: `eval_results\gated_attention\qwen3_next_random_prefix_n10_result.json`

```powershell
python -u experiments/run_latent_sensitivity.py --model Qwen/Qwen3-Next-80B-A3B-Instruct --task-type nested --difficulty sweet_spot --n-tasks 25 --n-latents 10 --control-mode random_noise --num-soft-tokens 2 --quantization 4bit --output eval_results/gated_attention/qwen3_next_random_prefix_n10_result.json
```

## Reporting Order

1. mean last-integer accuracy
2. EOS/completion rate
3. true generated-token count
4. strict final-answer accuracy
5. answer-anywhere accuracy
6. position-shift delta vs zero-prefix delta
7. oracle coverage after mean metrics
8. pairwise error overlap/correlation after oracle

## Claim Boundaries

- Do not treat mechanics smoke as gated-attention evidence.
- Do not interpret random-prefix lift without the position-shift and zero-prefix controls.
- Do not update article or README claim strength before mean-first report exists.
- Do not rebuild the pre-result freeze after primary results exist unless explicitly auditing.
