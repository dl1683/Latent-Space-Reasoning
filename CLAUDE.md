# READ AGENTS.md FIRST

If a local `AGENTS.md` is present in this directory, it holds the operating
process and wins over anything below. Read it before any work.

# Project

Native mathematics of latent spaces — see `README.md`, `STATE.md`, `NOTEBOOK.md`.
The prior program lives unmodified under `legacy/`; do not run or cite it as
current work. No GPU runs without explicit user approval.
