# Round 3 — NLM-002 as a competition between primitives

**Codex, 2026-08-27. Design skeleton only; not locked and not authorized to
run.**

## 1. Verdict that forces the pivot

NLM-001 is instrument-void for confirmatory claims because its required runtime
metadata were reconstructed post hoc. As a research decision it is a bounded
negative falsifier: lexical next-token KL lost its predictive-novelty contest to
a symmetric metric on contextual hidden states, its accepted labels were near
ceiling for leading predictors, directedness was not observed by the registered
endpoint, and its H2 statistic may recover the authored carrier taxonomy rather
than an invariant of the latent space.

NLM-002 will therefore not add words, models, paraphrases, or thresholds to
NLM-001. It will make two candidate primitives compete on common outcomes:

1. a single decoder/head-aware information geometry induced from response laws;
2. probe-indexed operational substitutability tested outside language models.

The non-LM realization will use cached DINOv2 image embeddings. DINOv2 is the
first choice over ESM-2 because multiple label and retrieval heads can be built
on cached embeddings with CPU linear algebra, and their predictions can be
checked against image labels that were not defined by those heads. The exact
cache, dataset, state slice, and labels remain implementation details to freeze
before this skeleton becomes a preregistration.

## 2. Why not diffusion or causal internal states first

The probabilistic/denotational diffusion foundation is a serious next
competition, but it changes both the primitive and the target from closeness to
evidence-update. A win could reflect the richer object rather than defeat of
relational closeness. Causal residual-state interventions improve the state
object, but remain inside a causal LM and retain the shared-decoder confound.
They should follow if both candidates below fail; they are not the cleanest
first adjudication of what NLM-001 exposed.

## 3. Candidate F — fixed response-law information geometry

### Primitive under test

For a latent state \(z\), each calibration probe or downstream head \(c\) gives
a predictive law \(K_c(z)\). Let \(J_c(z)\) be the Jacobian of its logits with
respect to \(z\), and let

\[
F(p)=\operatorname{diag}(p)-pp^\top.
\]

On calibration states and probes only, define one global pullback tensor

\[
G_F=\operatorname{mean}_{c,z}J_c(z)^\top F(K_c(z))J_c(z),
\qquad
d_F(z,z')^2=(z-z')^\top G_F(z-z').
\]

The implementation may use an exact, diagonal, or preregistered low-rank
factorization according to the CPU budget, but that choice and its rank must be
locked without held-out labels. \(G_F\) is one metric for every context. A
context may receive a monotone calibration map learned on training states; it
may not reorder pairs or fit a context-specific metric.

For the LM diagnostic arm, \(z\) is the inserted lexical embedding and the
Jacobian runs through the frozen decoder under calibration carriers. For the
DINOv2 arm, \(z\) is a cached image embedding and \(K_c\) is the output law of a
frozen CPU-trained linear head. Full Fisher geometry is the candidate; diagonal
and low-rank versions are approximation checks, not separate competitors.

### Hypothesis F

A single response-law geometry explains the stable ordering component that
NLM-001 called native. After monotone scale calibration, it predicts held-out
substitution behavior within 0.02 accuracy of the best probe-indexed relational
predictor, and the residual between-context reversal ratio does not exceed its
within-context replicate null.

### Decisive result for F

F wins only if both the LM diagnostic arm and the DINOv2 primary arm satisfy:

- held-out pairwise accuracy is within 0.02 of the relational predictor, with
  the paired 95% interval excluding a relational advantage larger than 0.02;
- on a common pair-support set, residual \(Q_F=B_F/W_F\le1.25\), with its 95%
  upper bound at most 1.5;
- no decoder/head-independent behavioral endpoint gains at least 0.05 from the
  relational profile after conditioning on \(d_F\).

That result kills context rank as a novel primitive for this program: it was
bookkeeping over ordinary response-law geometry.

### Kill conditions for F

F is killed as a sufficient primitive if, in both domains, the relational
profile beats \(d_F\) by at least 0.05 on the independent held-out behavioral
endpoint with a paired lower confidence bound above zero, while stable residual
non-collapse remains \(Q_F\ge2\) with a lower bound above 1.5. It is also killed
if its apparent success requires context-specific metrics, held-out label
selection, or a coordinate parameterization not transported under a declared
equivalent presentation.

## 4. Candidate R — cross-modal operational substitutability

### Primitive under test

The primitive remains the table \((X,C,N)\): states, admissible probes, and
probe-indexed substitution neighborhoods. It receives no inner product,
Jacobian, or metric. The empirical object is the full closeness profile

\[
\Phi_x(y)=\{c:y\in N_c(x)\},
\]

not the chromatic number alone. T2 and T3 are descriptive bookkeeping and are
not success criteria.

For DINOv2, \(X\) is a frozen test slice of cached image embeddings. Probe
blocks are distinct downstream questions available from the frozen labeled
cache—for example semantic class, coarse superclass, texture/style, and
retrieval relevance. Four independently trained heads or retrieval-index
resamples within a block serve as nuisance replicates, not as four new semantic
contexts. Heads train only on a disjoint training slice. Neighborhood labels
and magnitude thresholds are fixed from training data.

The held-out outcome is behavioral and external to the probe laws: whether
substituting candidate \(y\) for anchor \(x\) preserves a frozen label or
retrieval relation on test images under a held-out task family. The relational
predictor must be specified before outcomes are opened, must operate on the
calibration closeness profile, and must not use held-out head probabilities.

### Hypothesis R

Probe-indexed substitutability contains stable structure that a single
response-law geometry cannot represent, and this structure survives the move
from lexical LM embeddings to DINOv2 image embeddings. The structure must
survive head resampling, common-support analysis, and an independent behavioral
endpoint; merely rediscovering the names of downstream tasks does not count.

### Decisive result for R

R wins only if all of the following hold:

- between-probe reversals exceed within-block head-resampling reversals on a
  common pair-support set in both domains, with \(Q\ge2\) and a 95% lower bound
  above 1.5;
- the probe incompatibility structure is stable under state resampling, head
  retraining, and removal of each probe block in turn;
- the relational profile beats the fixed Fisher geometry by at least 0.05 on
  the decoder/head-independent held-out behavioral endpoint, with a paired 95%
  lower bound above zero;
- a task-block identifier, class composition, state norm, head entropy, and
  nearest-neighbor density baseline cannot recover the result within 0.02.

That result kills a single information geometry as a sufficient primitive and
earns only the bounded claim that operational pluralism transfers across these
two modalities. It does not establish a universal mathematics of latent
spaces.

### Kill conditions for R

R is killed as the program's leading primitive if the Fisher geometry matches
it within 0.02 and collapses residual \(Q\) in both domains; if the DINOv2
structure fails the head-resampling null; if it survives in the LM arm but not
the non-LM arm; if task identity or class composition recovers it within 0.02;
or if it does not improve an independently defined held-out behavior. A high
chromatic number, a high raw \(Q\) with tiny \(W\), or visually interesting
reversal witnesses cannot rescue it.

## 5. Mutual verdict table

| Result | Verdict |
| --- | --- |
| F matches within 0.02 and residual \(Q\) collapses in LM and DINOv2 | Kill R; adopt response-law geometry as the better closeness primitive |
| R wins by at least 0.05 on independent behavior and residual \(Q\) survives in both domains | Kill F as sufficient; retain cross-modal operational pluralism |
| F wins only in the LM arm | NLM-001 was decoder bookkeeping; neither primitive is yet general |
| R survives only in the LM arm | Kill R as a latent-space-wide primitive; the phenomenon is LM-specific |
| Neither predicts independent behavior | Kill both closeness candidates; move to denotational update on diffusion latents |
| Results depend on internal layer interventions | Open the causal-state competition as a separate preregistration |

There is no branch in which adding more lexical items rescues a losing
primitive.

## 6. Design and evidence gates before lock

1. Freeze the DINOv2 cache identity, encoder revision, dataset, state slice,
   label sources, train/calibration/test split, probe blocks, head architecture,
   head seeds, and every exclusion before fitting a head.
2. Freeze the LM model and tokenizer revisions and record every required runtime
   field automatically before the first forward pass. A missing field aborts;
   it is not reconstructed post hoc.
3. Define a common-support estimator for \(Q\). No cell-specific eligibility or
   omission of undefined cells may change the compared pair population.
4. Use paired or state-clustered resampling. Refit learned heads and the Fisher
   tensor inside each training replicate unless the reported interval is
   explicitly conditional on frozen fits.
5. Run the cheapest confound panel first: task/block identity, class balance,
   state norm, local density, head entropy, and an unlearned contextual cosine.
6. Keep LM and DINOv2 verdicts separate. No averaging may turn a modality
   disagreement into support.
7. Lock the analysis code, raw-artifact schema, exact thresholds, and falsifiers
   in `theory/EXPERIMENTS.md` before any outcome-bearing computation.

## 7. CPU feasibility

The DINOv2 encoder will not run: all image states come from the verified cached
embedding artifact. Frozen linear heads, retrieval indices, Fisher tensors,
distance matrices, and clustered resampling are CPU matrix operations. The LM
arm uses only the already demonstrated small-model CPU path, one system at a
time; it reacquires laws only if the exact Fisher statistic cannot be derived
from retained artifacts. No GPU is required or authorized. Before lock, the
implementation must publish a CPU memory bound and wall-time estimate and must
reduce state count or Fisher rank—not weaken the outcome or controls—if the
bound fails.

## 8. Claim boundary

The sentence at stake is not “cosine is wrong.” It is one of two narrower
outcomes: either a single response-law geometry suffices across two operational
domains, or probe-indexed structure predicts independent behavior after that
geometry fails. Anything weaker is an instrument result, not native
mathematics.

## Round 4 — navigation reframing

Both candidates now compete for the same navigation job: requirement (d), map. A
state \(x\) and move \(m\in M\) (substitution, interpolation, residual-state edit,
or composition) imply an endpoint \(B(m,x)\). F and R are candidates only if they
predict \(B(m,x)\) in the same calibration geometry and on the same held-out
behavioral task. Cost (c) is a useful side question only if this shared map job
is tied; at this lock step it is not the primary competition.

Reframed shared endpoint for this round: predicted consequence of an unmade move on
an independent downstream behavior in both arms.

Artifact-anchored operationalization (CIFAR-100, 6000/2000 split, seed 0):

Probe blocks:
- PB_fine: fine-label blocks for substitution and interpolation pairing.
- PB_coarse: coarse-label blocks for hierarchical substitution.
- PB_rgb_mean: mean-RGB quantile bins, label-free.
- PB_luma: luminance quantile bins, label-free.
- PB_edge: Sobel edge-density quantile bins, label-free.

Admissible moves:
- substitution(m): replace \(x\) by \(y\) from the same probe block.
- interpolation(m, λ): \(m_\lambda = (1-\lambda)x+\lambda y\), λ in a blocked
support interval, then query endpoints at unmade λ values.
- residual-edit(m,v): \(x+v\) where v is a sparse, bounded edit to the latent
residual state (LM arm only).
- composition: concatenate two legal moves and test associativity failures.

Independent endpoint:
- LM arm: fixed held-out continuation/evidence task from the NLM-002 pipeline,
already not used to fit either response-law or relational structure.
- DINOv2 arm: frozen held-out linear task head on one reserved behavioral target,
same 2000-test set, tested on move-perturbed states only.

Move instrumenting notes:
- The legacy `legacy/experiments/run_separatrix_probe.py` path/interpolation probe
is reused to define the unmade move grid and separatrix targets for both arms.

Fisher specification:
- DINOv2 linear head (exact): for state \(h\), logit \(z = Wh+b\), \(p=\mathrm{softmax}(z)\),
local Fisher \(F_p = W^\top(\mathrm{diag}(p)-pp^\top)W\). Pool over calibration
states/probe blocks for the arm Fisher.
- LM score-estimator: with \(S\approx64\) sampled tokens \(t_s\sim K_c(h)\),
\(g_s=\nabla_h \log p_\theta(t_s\mid h,c)\), and \(\hat G=\frac1S\sum_s g_s g_s^\top\),
then average \(\hat G\) over state-probe cells under common-support pairing.

## Claude — attack on round 4

The navigation list is the right skeleton. Its content re-imports ℝⁿ in three
places, and the endpoint makes F win by construction. In order of damage.

### B1. "Interpolation" is not a native move — it is the chart

`m_λ = (1−λ)x + λy` is a straight line in one coordinate presentation. It is
invariant only under affine changes of presentation, so by L5 it is not a
construct of the latent space at all. A denizen of the world does not get to
draw straight lines; a denizen gets the moves the world's **dynamics** permit.
In a causal LM the dynamics are the forward pass: a state is transported
layer-to-layer, and the sequence is extended token-by-token. In the DINOv2
world the dynamics are the encoder applied to image edits. So the native move
set is: (i) substitution (presentation-free), (ii) transport by the dynamics
(let the world move you), (iii) composition of (i) and (ii). Interpolation is a
*hypothesis* about the world — "is the chart-straight line a world-path?" —
and we already have data on it: the legacy separatrix probe found correctness
flickering non-monotonically along interpolation paths (26/35 tasks with
interior islands). Read natively, that is evidence that chart lines are **not**
paths of the world. That is the first navigation theorem target I want:
**characterize the chart-paths that are world-paths** — paths along which
probe consequences vary monotonically / commute with transport. Everything
else in "moves" follows from that.

### B2. "Cost" needs a currency the world defines

"Number of edits, entropy budget, probe-compute" are our accounting, not the
world's. Two native candidates, both measurable: (a) **consequence cost** — the
divergence in downstream behavior per unit move, i.e. how far the world's
response moves when I move (this is the Fisher pullback F, which is why F is a
cost candidate); (b) **description cost** — the information a denizen must
supply to specify the move in the world's own terms (tokens to steer from x to
y under the dynamics; for the LM this is measurable as the shortest prompt
that induces the substitution's consequences). (a) and (b) can disagree; where
they disagree is where native cost is not a metric. Pick one for NLM-002 and
say why.

### B3. The DINOv2 endpoint makes F win by construction

"Frozen held-out linear task head on one reserved behavioral target" — F is
built from linear heads' Fisher pullbacks; asking it to predict a linear head's
response to a move is asking it to predict itself. R, which never sees head
geometry, cannot win that contest even if it is the better map. The endpoint
must be **independent of both candidates' construction**: nearest-neighbor
retrieval in *raw pixel space* or under a *different* encoder, or a human
label that no head was trained on (CIFAR-100 fine label with heads trained only
on coarse and pixel-statistic blocks). The LM-arm endpoint ("fixed held-out
continuation/evidence task from the NLM-002 pipeline") does not exist yet;
define it or the arm is not lockable.

### B4. Common support for Q is still undefined

The audit's finding #6 stands: every Q so far conditions on cell-specific
eligibility. Define the common pair-support set once (pairs robust in every
cell compared) before any Q is computed, and report how many pairs survive.

### B5. What I will build once you answer

On the CIFAR-100/DINOv2 artifact, with no GPU: (1) the move-closure test —
for chart-straight lines between same-class and cross-class images, does the
encoder's response along the line vary monotonically (world-path) or flicker
(not a path)? This is the separatrix probe transported to a non-LM world and
is the cheapest test of B1. (2) The endpoint-independence check — heads trained
on coarse + pixel-statistic blocks only; fine-label kNN under raw pixels as the
independent endpoint. (3) F exact from the trained heads; R from substitution
profiles; both predicting the fine-label consequence of a substitution move.
Preregistration before any of it is scored.

## Round 5 — moves are what the world permits

### B1 — chart-straight interpolation is a hypothesis, not a native move

Concede. In this round, chart-straight interpolation is not added as a legal
move. Legal moves are exactly:
\[
\mathcal M = \{m_{\text{sub}}(x,y),\,T(x),\,m_2\circ m_1 : m_1,m_2\in\mathcal M\},
\]
where \(m_{\text{sub}}(x,y)\) is a substitution allowed by probe legality, and
\(T\) is the world transport map (LM dynamics step for language states, encoder
step for non-LM states).

Target theorem candidate:
\[
\mathcal P_{\text{world}}
=\{\gamma:[0,1]\to X:\forall c\in C,\ \mathrm{Commute}(c,\gamma)\land
\mathrm{Mon}(c,\gamma)\}.
\]
For chart path \(\gamma_{x,y}(t)=(1-t)x+ty\),

- \(\mathrm{Commute}(c,\gamma)\): for sampled \(0\le t_1<t_2\),
\[
B(c,\gamma,t_1,t_2)=
\mathbf{1}\!\left[B_c(\gamma(t_2))\neq B_c(T_{t_2-t_1}\gamma(t_1))\right],
\]
must be zero in average over probes and probe outcomes.
- \(\mathrm{Mon}(c,\gamma)\): the sequence of consequence labels
\(B_c(\gamma(t))\) is monotone (≤1 flip) on the sampled \(t\)-grid.

\(\mathcal P_{\text{world}}\) defines the operational world-path set. Legacy
separatrix data constrain the target directly: 26/35 tasks from `run_separatrix_probe.py`
showed interior correctness islands under chart interpolation, i.e. non-monotone
consequence with multiple transitions. That is incompatible with universal
`\(\gamma_{x,y}\)` world-path status and makes the hypothesis empirically
falsifiable in this preregistration.

### B2 — cost currency for NLM-002

Amend: choose **consequence cost** as the primary currency.
\[
\mathcal C_{\text{cons}}(x\to y)=d_F(x,y)
\]
where \(d_F\) is the Fisher pullback distance from the exact substitution-head
or LM-head Jacobian law after common-support pairing and fixed fitting.
\[
\mathcal C_{\text{desc}}(x\to y)=
\min_{m:\,m(x)=y}\operatorname{len}(m)
\]
is the secondary description-currency diagnostic: minimal description length to
specify the move in the native task machinery (prompt tokens for LM proxy or
editorial operations for non-LM residual edits when that arm is present).

They disagree where moves that are cheap to describe are not cheap in consequence
or vice versa. We will report the rank disagreement on the held-out substitution
set as \(\rho_{\text{Spearman}}(\mathcal C_{\text{cons}},\mathcal C_{\text{desc}})\);
non-positive or \(|\rho|<0.3\) is evidence that a single cost law is insufficient.

### B3 — endpoint lock (independence from both F and R constructions)

For this round, the non-LM branch is fully locked; the LM branch endpoint is
not yet lockable (no dedicated held-out LM continuation task is currently
frozen), so it is marked **unlockable until a non-LM-consistent continuation
endpoint is declared**.

For the locked CIFAR/DINOv2 branch:
- Artifact: CIFAR-100 cached DINOv2 embeddings, split \(6000/2000\), fixed
  encoder revision, fixed probe blocks, fixed seed.
- **Training heads for all F computations:** only `PB_coarse`, `PB_rgb_mean`,
  `PB_luma`, and `PB_edge` (label-free pixel-stat) blocks; no fine-label head is
  trained.
- **Held-out endpoint for both F and R:** raw-pixel nearest-neighbor in raw-image
  space and **fine labels only** on the reserved target of that endpoint.
  Concretely:
  1. Build a raw-pixel index on train images with their CIFAR-100 fine labels.
  2. For each perturbed test image \(z\), query \(k=32\) nearest by Euclidean
     distance in raw pixel space, break ties by raw id, and return the majority
     fine label.
  3. Prediction target is whether this kNN endpoint label equals the original
     unperturbed fine label (frozen, and not used in any head training).

This keeps \(F\) from predicting its own training object and keeps \(R\) from
using head labels it never saw.

### B4 — common-support estimator for \(Q\)

Let \(E_c(i,j)=1\) when a pair of states \((i,j)\) has well-defined consequence
labels for probe block \(c\) in both directions on the same admissibility grid
and in held-out task cells; else \(E_c(i,j)=0\).
\[
\mathcal S_U=\{(i,j):\prod_{c\in U}E_c(i,j)=1\}.
\]

For any block-pair comparison \(A,B\), compute reversals only on
\(\mathcal S_{\{A,B\}}\). Let \(q_x(A,B)\) be the fraction of admissible pairs in
that set with reversal for anchor \(x\). Then
\[
W=\operatorname{median}_{x,A}\; q_x(A,A),
\qquad
B=\operatorname{median}_{x,A<B}\; q_x(A,B),
\qquad
Q=B/W.
\]
Report \(|\mathcal S|/|\mathcal P|\) for each \((A,B)\) so missingness is
transparent and comparable.

### B5 — three CPU measurements in order

#### 1) Move-closure (non-LM world-path test)

- Hypothesis: chart-straight interpolation is mostly a non-world-path on this
  artifact; world-path fraction is materially below 1.
- Exact prediction: in the legacy 35-task separatrix geometry, interior islands
  (\>1 consequence transition or flicker) should appear in \(\ge 60\%\) of
  interpolation families; this run must show nontrivial flicker before any map
  claim.
- Kill condition: flicker fraction \(\le 5\%\) with bootstrap CI above/below
  threshold for both same-class and cross-class probe pairings (chart lines would
  then be treated as the default transport path).
- CPU cost (bound): dominated by cached-embedding path probes,
  \(O(N_{\text{pairs}}\,L\,d\,|C|)\), expected under 1 hour on 16 cores with BLAS
  on full 2000-test/6000-train pairing.

#### 2) Endpoint-independence control

- Hypothesis: fine-label behavior is recoverable under a label-free endpoint and
  remains unchanged relative to any head-dependent endpoint proxy beyond \(0.02\)
  absolute accuracy.
- Exact prediction: replacing endpoint probe heads with raw-pixel \(k=32\) fine-label
  kNN changes held-out score by \(\le 0.02\), and head training remains on coarse +
  pixel-statistic blocks only.
- Kill condition: endpoint substitution effect \(|\Delta\text{acc}|>0.02\), or
  any sign of fine-label leakage into F-head training (even indirectly through
  shared feature selectors).
- CPU cost (bound): embedding-level index build/query plus kNN vote, \(O(N_{\text{train}}d + N_{\text{test}}N_{\text{train}}d)\) and
  one nearest-neighbor pass per run.

#### 3) Fixed-geometry competition on the same endpoint

- Hypothesis: one of \(F\) (exact trained-head Fisher pullback) or
  \(R\) (substitution-profile world-path predictor) dominates held-out
  substitution outcomes on fine-label raw-pixel endpoint.
- Exact prediction: report paired means on the same pair-support set; declare:
  - \(F\) wins if \(\Delta_{F-R}\ge 0.05\) with paired 95% lower bound \(>0\),
    and residual \(Q_F\) remains near collapse;
  - \(R\) wins if \(\Delta_{R-F}\ge 0.05\) with paired 95% lower bound \(>0\).
- Kill condition: neither side has paired margin \(\ge 0.05\), or both are within
  0.02 while residual \(Q\) diagnostics are unstable/unsupported.
- CPU cost (bound): all CPU, Fisher construction from cached Jacobian summaries and
  pairwise matrix operations; expected minutes for one prereg batch plus cluster
  resampling.

## Round 6 — non-LM branch adjudication and path semantics

- Preregistration outcome: `NLM-002` non-LM branch is partially scored. M2 endpoint-independence lock is triggered by the data (`raw-pixel k=32` endpoint accuracy 0.115; embedding-kNN 0.761; agreement 0.1205), so the lock does **not** support a primitive verdict.
- `F` vs `R` competition is currently non-informative (`\Delta_{F-R}=-0.0036`, CI [-0.0343, 0.0261], n=16,660).
- M1 chart-path closure is the operative signal: same-class paths are mostly monotone at coarse readout (`PB_coarse` flicker 2%, CI [0.3%, 3.7%]), while cross-class chart paths flicker strongly (`PB_coarse` 18.3%, `any_readout` 78%).
- Rule on implementation decisions:
  - **Decision 1** (embedding-space readouts for path interpolation) is kept; this was required for comparability and is still valid.
  - **Decision 2** (substitution move framing) is kept as procedure but not yet a valid primitive claim because endpoint noise prevents F-vs-R interpretation.
- Guiding-question next step: keep the endpoint independent (true fine label, no head training), but only run primitive comparisons after replacing the consequence target with a fixed, still-independent endpoint that is at least minimally informative.
- Map-path consequence of M1: define same-class chart paths as provisional *world-paths* and cross-class chart paths as non-world transport on this artifact; future rounds should report claims only within the pre-qualified same-class regime unless a different world-path predicate is added.

## Round 7 — NLM-003 lock and path predicate

- Register `NLM-003` lock on the existing `NLM-002` artifact (cache `8de4f0b0`).
- Consequence target is now `true fine label` (no fine-label head training), keep the same `F` and `R` constructions, and retain `cosine` + `euclid` baselines.
- Predicted directional target is `Delta_{F-R} = -0.05` (R advantage); score only on common-support pairs with anchor bootstrap inference, and use M1 world-path gate as before.
- Keep a hard caveat: coarse-head near-monotonicity is weak by construction; preserve nontriviality by testing the world-path predicate with nonlinear kNN readouts (12.7% same-class vs 38% cross-class flicker).

## Round 8

NLM-003's original table appeared to pass its narrow lock: `R` beat `F` on the
true fine-label endpoint with `Delta_{F-R} = -0.104 [-0.148, -0.058]`, over
6,199 scored pairs. The required diagnostic rerun changes that interpretation.
With the coarse head removed, `R` is 0.586 while `F` is 0.667
(`Delta_{F-R} = -0.095 [-0.142, -0.049]`). The R win was a taxonomy leak:
fine labels nest inside the coarse classes carried by the original profile.
The R-over-F directional claim is withdrawn.

What remains is the chart comparison. Cosine and Euclidean still dominate both
leak-free native candidates on the same 129 supported anchors, so the result is
directional evidence about the tested instruments, not evidence that profile
continuity is the native map. The adjudication is now: **NLM-003 is a corrected
narrow instrument comparison; no tested native candidate earns replacement
status.**

The guiding question makes the result plain. In this world, the best measured
map for fine-label consequences is the imported DINOv2 chart metric: a denizen
gets the best prediction by treating chart-nearness as informative. That may be
because DINOv2 pretraining shaped the representation so visual regularities are
already laid out in its coordinates. Is that already the native geometry because
the encoder was trained to make it so? **No, not in the strong sense:** it does
not show that the chart is intrinsic, and the encoder was not trained on these
CIFAR fine labels. The
failure world is one where the same states are nonlinearly re-charted, the move
is composed or out of distribution, and cosine/Euclidean neighborhoods stop
predicting consequences while a transport/profile map remains stable. There,
the chart was a convenient view and not the world's geometry.

Next measurement: hold the cache and true fine-label endpoint fixed, apply one
predeclared nonlinear invertible reparameterization fitted on calibration states,
and compare cosine, Euclidean, `R`, and `F` on held-out composition and
out-of-distribution substitutions over the full 400-anchor support accounting.
The decisive result is a chart lead of at least 0.05 over the best native
candidate on both move families with at least 80% anchor support (chart remains
operationally native) versus a lead of 0.02 or less, or a native lead of at least
0.05 (the present chart advantage was coordinate- or task-specific).

## Round 9

NLM-004 adjudicates the random-init null against the ledger registration. The
registered point-estimate thresholds are met: cosine fine-label consequence
accuracy is `0.575` in the null versus `0.946` trained, a gap of `0.371`, and
embedding-kNN is `0.069` versus `0.761`. This is not a claim that a random
network has no image signal: the null RGB and luma heads still reach `0.8335`
and `0.8205`, while its coarse head reaches only `0.2075`. The cheap statistic
heads survive; the trained semantic chart does not survive randomization.

The path result makes the guiding-question implication stronger. Null
same-class fine-kNN chart-path flicker is `95.3%` (`k=32`; `97.7%` at `k=8`,
`86.7%` at `k=128`) versus `12.7%` in the trained artifact. Training therefore
made both ordinary chart nearness predictive and chart-straight paths coherent.
A denizen of the trained world can use the coordinates as a map and as a first
path rule. But this remains learned chart regularity, not a chart-independent
law: the path still imports vector interpolation, and the readout is tied to
the trained representation. The result is recorded as supported exploratory
null-world evidence because the artifact does not report the registered
anchor-bootstrap CIs; Claude's authorship of the preregistration is not the
reason for the qualification.

### NLM-005 lock

The next gate is composed substitution and transport. The world is the same
CIFAR/DINOv2 image-to-embedding system, with the true fine-label endpoint and
the NLM-003 support layout fixed. Substitution is `S_y(x)=y`. Transport is an
image-world operation: horizontal reflection or one-pixel right translation
with fixed padding, followed by re-encoding with the frozen encoder on CPU.
For each anchor/candidate/edit, compare the order-conditioned pairs
`ST: x -> y -> T_e(y)` and `TS: x -> T_e(x) -> y`; direct unedited substitution
and direct transport are controls. Score cosine, Euclidean, `F`, and `R`
without the coarse head against the inherited fine-label relation, with exact
ties and anchor-bootstrap intervals.

The hypothesis is that chart training gives one-step/path coherence without a
native composition law. The preregistered native-rescue prediction is an
ST-versus-TS chart ranking change of at least `0.05` on one edit family plus a
`>=0.05` native lead over the best chart metric on that order. A chart lead of
`>=0.05` on both orders with `>=80%` support kills the rescue; an order gap
below `0.02` or support below `80%` is non-diagnostic. Re-run NLM-003 with
`R` without coarse, exact tie counts/cross-tabs, and kNN-fine M1 at
`k={8,32,128}`; the `nlm003_v2_diagnostics` rerun is sensitivity only, not new
evidence.
Two edits over 2,000 held-out images cost about 140 seconds of re-encoding at
35 ms/image, plus at most ten minutes CPU scoring.

## Round 10 — the residue of training and the next world

NLM-005 is void. Only 129/400 anchors had common support (`32.25%`), below the
locked `80%` requirement, so the result cannot support either native rescue or
general chart survival. The hflip ST−TS gaps are all at most about `0.006`; the
shift1px chart gaps are similarly near zero, although `R_no_coarse` reaches
`0.027` on that sensitivity row. Cosine remains about `0.32` ahead of the best
native candidate on every scored order. The important design fact is that both
edits are augmentations the encoder was trained to make invariant to: they are
near-identity moves in its learned world. Forty random candidates over 100
classes also cannot be expected to reach 80% support.

After five measurements, close the frozen-encoder closeness/map line as a
program. Its residue is precise: a trained world supplies a chart metric that
predicts one-step consequences and chart-straight routes; the same structure
collapses in an untrained chart; and no native construct tested so far competes
after the coarse taxonomy leak is removed. A denizen inherits navigation
equipment from training. That is an operational map, not an intrinsic law.

The replacement, NLM-006, is a stratified audit of transports outside the measured
invariance class: large crops, color inversion, mixing, and occlusion. Freeze
the edit families and candidate identities before scoring; use 20 same-fine-class
and 20 cross-class hard-negative candidates per anchor; verify non-near-identity
embedding displacement on calibration images; and report support by stratum.
The hypothesis is that at least two of four such families will either break the
chart's held-out consequence ranking or reveal a transport-aware predictor that
beats it. Support the hypothesis with a `>=0.05` native lead, or a chart lead
collapsing to `<=0.02` under composition while the direct control remains
stable, at `>=80%` support. Kill it if the chart keeps a `>=0.05` lead on every
valid family. Void any run with in-class edits, post-score candidate changes,
less than 80% support, or endpoint leakage. Four edits over 2,000 images cost
about 280 seconds of CPU re-encoding plus at most ten minutes of scoring.
