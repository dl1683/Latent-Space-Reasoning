# Diffusion Error-Function Geometry

This file is generated by `experiments/analyze_diffusion_error_function_geometry.py`.
It turns the current diffusion repair rows into explicit mathematical claims about which error functions should be optimized next.

## Inputs

- Repair-value geometry: `eval_results\diffusion_language\diffusion_repair_value_geometry.json`
- Phase-source targets: `eval_results\diffusion_language\diffusion_phase_hybrid_loss_targets.jsonl`

## Derived Assertions

| ID | Assertion | Evidence | Error-Function Consequence |
| --- | --- | --- | --- |
| `E1` | Raw repair lift is not the same as cost-profitable repair value. | 5 raw positive repair targets shrink to 3 cost-profitable targets at lambda 0.180000; flipped tasks: `plan_001`, `plan_003`. | Use a utility loss with an explicit compute penalty, not a binary repairability label. |
| `E2` | First repairable denoise step is not sufficient to predict value. | The earliest step `10.000000` contains mixed-sign value rows: `plan_001`, `plan_003`, `plan_004`. | The geometry vector must include source quality, prompt gap, and feasibility terms. |
| `E3` | The current spend surface is separable by label-free value geometry. | The runner value proxy selects `plan_004`, `plan_006`, `plan_007` with regret 0.000000; source-quality band gap is 0.022857. | Train the next selector on feature-family stability, then test transfer instead of reusing thresholds as theory. |
| `E4` | Denoise history is source evidence only after retention and source-advantage checks. | Naively trusting every repairable history source creates 4 false positives; even any-safe-history trust creates 3 false positives. | Use a source-trust loss separate from the repair-spend loss. |

## Repair-Value Geometry

- Cost penalty lambda: `0.180000`
- Raw positive repair targets: `5`
- Cost-profitable repair targets: `3`
- Cost flips: `plan_001`, `plan_003`
- Early-step mixed-sign tasks: `plan_001`, `plan_003`, `plan_004`
- Runner value-policy regret: `0.000000`
- Runner value-policy selected tasks: `plan_004`, `plan_006`, `plan_007`

## Source-Trust Geometry

- Source-choice targets: `5`
- Trust-history positives: `1`
- Preserve-final targets: `4`
- Naive repairable-history false positives: `4`
- Any-safe-history false positives: `3`
- Mean retention-safety lag: `12.750000`

## Candidate Error Functions

### Cost-Aware Repair-Value Loss

`L_value = BCE(1[Delta S / c > lambda], h_value(phi(p, x_t))) weighted by |Delta S - lambda c|`

Predict whether another denoise repair is worth the extra GPU generation before spending it.

Current evidence: `DIFFUSION_BUDGET_POLICY_LOSS.md` and `DIFFUSION_REPAIR_VALUE_GEOMETRY.md`.

Next test: Train a small selector on generated repair-value targets and evaluate on a new lean planning slice.

### Retention-Gated Source-Trust Loss

`L_source = WBCE(trust_history_source, h_source(retention, similarity, source_advantage))`

Separate repairability from the decision to use denoise history as the source state.

Current evidence: `DIFFUSION_PHASE_HYBRID_MECHANISM_AUDIT.md` and `diffusion_phase_hybrid_loss_targets.jsonl`.

Next test: Replace hand-coded source thresholds with a learned trust-history selector.

### Composite Denoise Reasoning Loss

`L_total = L_value + beta L_source + gamma L_ret + eta L_realization`

Make reasoning improvement a constrained optimization over value, source trust, retention, and anchor realization.

Current evidence: `DIFFUSION_ANCHOR_RETENTION_LOSS.md` and `DIFFUSION_REALIZATION_QUALITY.md`.

Next test: Report all four terms for every promoted operator and check which term predicts transfer failures.
